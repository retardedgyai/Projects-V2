# 港町建築リワーク

Creator contract: 到着桟橋 → 市場 → 高台の集会所を、提供された港町画像の構図を読み替えて作る。
Minecraft Vanillaのブロック建築。機能・経済・職業・UI・保存形式には手を入れない。
作業branch: play/gyai/harbor-architecture。

## 合格を判断する証拠

- 実際に生成した街をVanillaクライアントで表示した複数のプレイヤー目線の画像。
- 桟橋: 船体と荷揚げ場を前景に、軒の連なり越しに赤い大屋根が見える。
- 市場: 柱・梁・軒・石基壇がつながり、巨大な無装飾壁や宙に切れた鎖がない。
- 集会所: 地形から建物が立ち上がり、入口・階段・バルコニーが実際につながる。
- 同じ意匠規則を工房・倉庫・学舎・外周街区・船に展開する。単なる同型反復にはしない。
- 既存5施設のinteraction block、接近可能性、桟橋、上階への階段と頭上空間を自動検査。
- 画像を見て問題を修正する。テスト成功や俯瞰の簡易レンダーだけでは見た目合格にしない。
- Hypixel/Wynncraft相当は目標であり、コード行数や装飾数で達成を宣言しない。

## 設計

既存施設はy40の使いやすい短い回遊路に残す。背後の大集会所はy52の石造テラスに載る。
暖かな砂岩・石、暗い木組み、白い塗壁、酸化前の銅/レンガの赤屋根を主材にする。
大屋根・中規模の商館・小さな庇の3段階。窓を一段奥へ引き、張り出す軒とバルコニーで陰影を作る。
先に基壇・道路・建物、その後に用途が読める付属物を配置する。

## レビュー環境

人間Creatorのセッションは操作しない。保存機能のない独立した建築プレビューサーバーに、
レビュー専用アカウントを所定のカメラ位置で接続して撮影する。ゲームのmanual smokeとは区別する。
テスト用画像は実景と明記し、概念画像や立方体の簡易描画とは混同しない。

## 状態

2026-09-07、建築実装・動線検証・実ゲームでの外観レビューを完了。
完成時の判断と証拠は末尾の監査を参照。以下の各反復に書かれた未完了事項は、その時点の履歴。

### 第2反復 / 2026-09-06

- 平坦な敷地＋独立した大屋根を廃し、実基壇・木組み商館・開放店舗・屋上回廊へ変更。
- 第一反復の実景で左右同型が目立ったため、倉庫は横向きの棟＋荷揚げ窓へ変更、学舎も幅を縮めた。
- MinestomではVanillaの隣接更新が走らないことを踏まえ、柵・壁・板ガラスの接続状態を明示。
- 宙に分離した船の斜め索具を連続したブロック列へ。植込み・階段・集会所床の支持を修正。
- 主通路へ出た杭と、拡幅した階段へ重なる旧本棚を検査で発見・修正。
- 5施設の座標/interaction、5ブロック幅の到着動線、従来の北側倉庫/市場接近路、
  5幅の階段12段、階段方向・頭上空間・基礎、上階内部への到達、手すり接続を検査。
- `:server-minestom:test :server-minestom:distZip` 成功。全588 tests / failures 0 / errors 0。
- 25575の独立プレビューにVanilla26.2を接続。手動キー/マウス操作をせず、別名の固定スポーンで実景撮影。
- 既存25565のゲームサーバー/保存形式/ユーザーセッションは変更していない。配布zipは更新、live installは未更新。

実景画像（生成イメージではない）:

- `assets/harbor-architecture/market-r1.png`: 初回の正面、市場。対称性が強すぎる。
- `assets/harbor-architecture/arrival-r2.png`: 倉庫の横棟、手すり修正後の桟橋。
- `assets/harbor-architecture/gallery-r2.png`: 上階回廊。旗の厚み、階段出口で削れた窓下の壁が次の修正対象。
- `assets/harbor-architecture/quay-r2.png`: 支持されたクレーン、岸壁、連続した船の索具。

次の品質課題: 厚い羊毛製の旗、回廊開口の納まり、敷地外周の自然地形と建築の接合、
商館以外の用途別ディテール、居住階へのアクセス。目標水準の達成は宣言しない。

### 第3〜4反復 / 2026-09-06

- 旗を厚い羊毛からVanillaの壁掛け旗に変更。回廊の階段出口で窓下が削れていた箇所を閉じ、正面入口へつないだ。
- 倉庫・交易所の2階とバルコニーに実階段、側面の路地入口、執務用の家具を設けた。
- 階段はすべて「最終段の上端＝踊り場の上面」になるよう修正。旧来の下段y40開始では
  最上段と踊り場が1ブロック離れていたため、下段y41開始へ変更し、主階段の段数12は保持。
- 街の主要動線に階段の基礎を出さない。上下階の入口、手すりの基礎、西側小階段の途中踊り場も検査。
- 街を侵食しない独立した岬の地形を生成。岩棚・急な岩壁・植生のある上面と、曲がったヤシを配置。
  海側はWarm Ocean、陸側はJungleのVanillaバイオーム。独自テクスチャやクライアント変更はなし。
- 港のview distanceは10。新しいチャンクの地形も同じ決定的な規則で生成される。
- 撮影時に視点/インベントリが変化するケースがあり、Creatorへ確認して操作中でないことを確認。
  専用previewだけはカメラを固定し、撮影を妨げるinventoryを閉じる。通常のゲームサーバーへは適用しない。
  `projects.harbor.preview.lockCamera=false` を指定した独立previewではこの制限を外せる。
- 最新状態で `:server-minestom:test :server-minestom:distZip` 成功。589 tests / failures 0 / errors 0。
- 比較に使えないメニュー付き写真はローカルの生成物ディレクトリへ退避し、正式なレビュー画像には含めない。

検証済みの実景:

- `gallery-r3.png`: 回廊の壁・旗の修正を同じ座標から確認。
- `market-r3.png`: 市場から見た建物の重なり、幅を変えた学舎と小さな露店。
- `overview-r3.png`: 岬と水色を加えた全体構成（俯瞰、プレイヤー目線の代替ではない）。
- `overview-r4.png`: 階段・路地入口の修正、view distance 10を含む現コードの俯瞰。
- `arrival-r4.png`: 現コードをVanilla26.2で表示した到着桟橋の実景。今回の基準画像。

全画像は `assets/harbor-architecture/`。旧 `assets/harbor-ascension/harbor-preview.png` は旧版の簡易立方体描画で、
最新建築の完成証拠には使用しない。撮影専用サーバー/クライアントは撮影後に停止し、ゲーム側25565は維持。
live installは旧版のまま。最新zipを生成したが、Creatorの通常環境へ無断適用/再起動はしていない。

### 次に進めること（目標は継続）

1. 工房・交易所・学舎・修船場を、共通商館の色違いから用途ごとの輪郭へ作り分ける。
2. 2隻の船の長さ/船体/帆装を分け、岸壁と荷揚げ・修船の関係をより具体的にする。
3. 建物の軒下や外壁の単調な面、外周の直線的な基壇と岩場の接合を詰める。
4. 基準の桟橋/市場/回廊を再撮影してから、同じ品質を外周街区へ広げる。
5. 上記の視覚面を実景で確認するまで、Hypixel/Wynncraft相当の品質目標を達成したとは扱わない。

### 第5〜6反復 / 修船場と小型船

- 東の外周商館を、長い換気棟・開放架構・船体の肋骨・吊り上げ梁・海側の作業床を持つ修船場へ変更。
  街路に面する側通路は残し、柱脚と梁を連続させた。主要5施設の配置は不変。
- 東の船を短い一本マストの小型船に変更。低い船尾操舵室、縦帆、前方の三角帆により、
  西の大型二本マスト貨物船とは長さ・帆の向き・輪郭が異なる。
- `overview-r5.png` は上記を反映した実景俯瞰。`shipyard-r5.png` は修船場の実際のプレイヤー目線。
  近景で屋根端の階段ブロックが鋸歯状に見えたため、半ブロックの連続勾配と暗い木の縁へ修正。
- 独立previewへ `HarborShipyard` の固定撮影点を追加。通常ゲームのカメラ制御には影響しない。
- 修船場の柱脚・側通路、小型船のマストと船体長を回帰検査に追加。
  海面の検査座標はgeneratorの上端排他に合わせてY=38（Y=39は空気）とした。
- 工房・学舎・交易所のシルエット差と、主要街区の外壁/屋根の単調さは未解消。
  本変更だけで全街区の品質目標達成とはしない。
- `shipyard-r6.png` で修正した屋根端を同じプレイヤー目線から確認。
  `arrival-r6.png` で到着桟橋の構図を再確認。すべてVanilla26.2の実表示を撮影したもの。
- 最終コードで `:server-minestom:test :server-minestom:distZip` 成功（589 tests、失敗/エラー0）。
  撮影用クライアントと25575のpreviewは停止。通常ゲームの25565とlive installは変更していない。

### 第7反復 / 鍛冶工房の輪郭

- 既存座標の工房を、2階バルコニー付き商館から、高い一室の作業場と2連の石造アーチへ変更。
  横向きの低い屋根と浅い前屋根により、右の学舎/手前の交易所/奥の集会所との高さを分ける。
- 炉の覆いから煙突まで実ブロックを連続させ、暖色ガラスと鉄格子の高窓で住宅と用途を分ける。
  Minestomで必要な鉄格子の接続状態も、港町のブロック配置処理内で明示する。
- 鍛冶工房のsmithing table/anvil/crafting tableの座標と他4施設は不変。
  新たに2つの開口、柱脚〜アーチ頂部、廃した住居階の空間、鉄格子の接続を回帰検査。
- `HarborFoundry` は独立previewだけの固定撮影位置。通常プレイヤーの操作や保存状態には介入しない。
- `foundry-r7.png` の近景で石アーチ・作業台・炉の関係を確認。`market-r7.png` では左右の高さの差が見える。
  ただし中央の受付と集会所の切妻が重なっていたため、受付を低い寄棟のロッジへ再設計。
  受付のカウンター座標、5幅の正面入口、柱の支持を保持し、受付背後の集会所を主役にする。
- `market-r8.png` で寄棟への変更を同じ視点から確認。奥の集会所入口と回廊が見えるようになった。
  工房/受付の変更は小物の増量ではなく、用途・高さ・屋根の向きによる構図の変更。
- `arrival-r8.png` は同じ最終コードの到着桟橋。受付越しに回廊・集会所入口・階段の関係が見える。
- 最終コードで全589テスト（失敗/エラー0）とdistZip成功。通常ゲームの25565/live installは未変更。
  撮影用クライアント/25575だけを停止して検証を終えた。
- 未完了: 学舎と交易所の独自性、街全体の外周基壇と自然地形の接合、集会所の細部と内外の居場所。
  通行テストの成功を、街全体の視覚品質合格の代わりにはしない。

### 第9反復 / 学舎と外周地形

- 学舎を、開放された講義室と八角形の高い書庫棟に分けた。小さな青緑の銅屋根は赤い集会所と輪郭/材質が異なる。
  書庫棟は内部を空洞にし、床レベルの入口で講義室へ接続。上階の見せかけバルコニーは作らない。
- 熟練の手引きのinteraction blockは変更なし。装飾用本棚/椅子のみを新しい室内の動線へ合わせた。
- 外周石垣の海側だけに岩棚を生成。主要街路や桟橋へは配置せず、岩棚/植生で人工基壇の下端を自然地形につなげる。
- 学舎入口、書庫への実際の到達、内部空間、尖塔の支持、岩棚と開水面を回帰検査。
- `academy-r9.png` のプレイヤー目線では講義室の側面が大きな無地の壁として露出していた。
  これを修正対象として記録し、高窓と窓枠で分割。後方の窓は主階段へ庇を出さない。
- `overview-r9.png` は側窓修正前の実景俯瞰。書庫棟の屋根、修船場脇の岩棚、既存の開水面を確認。
- 交易所の計数室へ、岸壁を見下ろす張り出し出窓を設けた。支える梁と持ち送り、2階からの入口がある。
  既存のバルコニー入口（x16〜17）は塞がず、出窓内部の床と頭上空間を追加検査。
- `academy-r10.png` を同じ撮影位置で比較し、街路側の大きな無地の壁を高窓で分割できたことを確認。
- `arrival-r10.png` で交易所の出窓が岸壁側へ張り出す輪郭と、維持した正面動線を確認。
- 主要な建物の用途別輪郭は一通り作り分けたが、全体品質目標は継続。
  次は集会所内部の梁構造/居場所と、水際の建築・桟橋・船のつながりを実景で監査する。
- `overview-r10.png` は最終コードの実景俯瞰。近景の代替ではなく、屋根と海岸線の全体構図の確認に使用。
- 最終コードで全589テスト（失敗/エラー0）・distZip成功。撮影専用のサーバー/クライアントのみ停止。
  main、通常ゲームの25565、live install、ゲームの保存状態は変更していない。

### 第11反復 / 集会所の内部と乗船動線

- `hall-r11.png` の実景で、集会所の内部が暗く、屋根を支える構造も乏しいことを確認。
  柱・持ち送り・横架材・垂木・棟をつなぐ3組の架構、そこへ鎖で吊るす灯具を設けた。
  左右の卓を内側へ寄せ、壁の間に石造の炉を配置。5幅の中央通路は保持。
- `berth-r11.png` は変更前の船側の実景。船室上のランタンが半ブロック浮いていることも発見し、屋根を修正。
- 貨物船を岸から8ブロック離して船首と岸壁の桟橋の重複を解消。中央桟橋から両船へ有効3幅の通路を設置し、
  舷側の乗船口へ接続した。甲板まで段差なし・2ブロック高の空間で到達することを検査。
- 配置後の柵接続処理と実ブロック出力の範囲をz58まで広げ、移動した船尾も含める。
- 固定の `HarborHall` / `HarborBerth` 撮影位置は独立preview専用。通常ゲームの操作には介入しない。
- `hall-r12.png` では明るさが改善した一方、低い木製の灯具が視界を大きく占めた。
  実景での比率に合わせて3ブロック上げ、枠を細い鉄格子へ変更。照明数を増やす対応にはしない。
- `hall-r13.png` は調整後。照明が低く視界を占める問題を解消し、梁と通路が見える。
- `berth-r12.png` は船を移動し連絡桟橋を接続した状態。`boarding-r13.png` は乗船口に向けた
  実際のプレイヤー目線で、連絡桟橋の幅・手すり・段差なしの甲板接続を確認したもの。
- 集会所内部の中央通路、屋根支持、吊り照明の接続、両船への到達、3幅の連絡路、岸壁の解放を回帰検査。
- `overview-r13.png` は最終コードの実景俯瞰。船の位置と連絡桟橋を含む全体構図を確認。
- 最終コードで全589テスト（失敗/エラー0）とdistZip成功。撮影専用のサーバー/クライアントを停止。
  通常ゲームの25565/live install/main/保存データは未変更。
- 品質目標は継続。正面以外の路地・裏手の構図、集会所背後に見える大きな岩壁などは仕上げの監査対象。

### 第14反復 / 背景地形の切断面と西路地の監査

- Creatorから撮影継続の了承を受け、独立previewの固定位置 `HarborRear` / `HarborAlley` を追加。
  通常ゲームのクライアント操作や保存状態には介入していない。
- `rear-r14.png` は修正前。集会所裏の芝地y51に対し、生成保護境界z-62の直後で丘がy64まで
  跳ね上がり、広い平らな石の断面を露出していた。小物で隠すのではなく、境界外の丘の裾を修正。
- `HarborBackdrop.height` で町側の標高から遠景の元の丘へ滑らかに接続する。
  高い面の切り下げと低い部分の埋め戻しの両方を行い、左右の海岸崖・遠景の山頂・町の生成除外範囲は維持。
- `rear-r15.png` は同じ位置の実景比較。直線的な石の断面がなくなり、起伏のある斜面と植生につながった。
  最初の切り下げのみの案は東端に段差を残して回帰テストが失敗したため、採用せず接続式を修正した。
- `alley-r14.png` は倉庫西の実景。両側の軒、開口、奥の工房という重なりがあるため、大改造や小物追加はしない。
  倉庫側入口から工房脇まで既存西路地がつながることを、生成後の到達性テストへ追加。
- 裏の境界高さ、丘の裾の隣接標高差、既存の町/海上生成除外と遠景山頂の高さを回帰検査。
- `hall-r15.png` は室内からの最終比較。奥の開口は灰色の断面ではなく、植生と斜面を切り取るようになった。
- 最終コードで全589テスト（失敗/エラー0）とdistZip成功。西路地の追加到達性検査も合格。
  処理は `HarborScene` → `HarborBackdrop.generate` → 境界補間した `height` → 実ブロック配置。
  裏の地形接続が崩れた場合は `HarborBackdrop.height` と `HarborSceneTest` の境界検査を最初に確認する。
- 品質目標は継続。今回解消したのは背景の切断面であり、街全体の目標品質を満たしたという判定ではない。
  岸壁から見た屋根/帆/植生の輪郭や、裏側を含む建物の作り込みは引き続き実景で評価する。

### 第16反復 / 市場を建物につながる街路へ

- `market-r16.png` で現行の主動線を再確認。施設の建物は作り分けられた一方、中央の石畳に対して
  屋台が独立した小さな屋根に見え、参考画像のような建物と市場の重なりが不足していた。
- 倉庫の街路側に、3組の柱と垂木で支える片流れの木造回廊を増築。中央5幅と全幅の横断路を守り、
  回廊自身にも3幅の通路を持たせた。倉庫への側面入口と組み付けの荷造り台がある。
- 交易所側は同じ回廊を反転配置せず、3点で張る三角形の帆布とした。こちらも側面の実入口へ接続する。
  倉庫/市場のinteraction block、北の既存入口、上階への階段は変更しない。
- `market-r17.png` / `arrival-r17.png` は最初の実景。左の回廊で前景ができた一方、右の羊毛の布が
  厚い立方体の階段に見えたため、その形は最終採用しない。
- 帆布は標準の `BLOCK_DISPLAY` を薄く傾けた静的な面に変更。バニラの羊毛と黄土色のテラコッタの
  テクスチャを使用し、独自テクスチャ/クライアントmod/毎tickのアニメーションを追加しない。
  支柱と建物への固定梁は実ブロック。表示Entityは `HarborScene.Result.scenery` に保持し、テスト終了時に破棄。
- `HarborScene` → `HarborDistrictArchitecture.marketStreet` で回廊/側面入口/帆布を構築し、既存の家具と道を配置。
  不具合時は `marketStreet` と `HarborSceneTest` の側面入口/3幅回廊/横断路/帆布の厚さの検査を確認する。
- ブロックTSVは地形/建築ブロックのみの診断出力。薄い帆布の表示は含まれないため、完成像の証拠にはせず、
  Entityのmetadata検査と実際のVanilla写真で確認する。
- `canopy-r18.png` で薄い帆布の近景を確認した後、重すぎた角柱を細い支柱へ変更。
  布の隣接面も重ねず、共面の重なりによるちらつきを避ける。支柱の地面から頂部への連続と固定梁も検査する。
- `canopy-r19.png` は細い支柱、`arcade-r19.png` は木造回廊内の実景。倉庫側面の入口、荷造り台、照明と
  架構がつながって見える。帆布とは異なる、実際に歩ける屋根付きの空間になった。
- `arrival-r19.png` では布が真横に近く細い線に見えたため、奥行き方向の勾配を追加。
  タイルの回転と配置座標を同じ平面に合わせ、面の継ぎ目を維持する回帰検査も追加した。
  `arrival-r20.png` で到着桟橋から帆布の面が見えることを確認。集会所正面と5幅の主動線は隠していない。
- `canopy-r20.png` は最終傾斜の近景。薄い布・細い支柱・側面入口を確認。手前の街路からは布の下面が
  浅い角度で見えるが、桟橋側からは上面が見える。回廊本体は `arcade-r19.png` から変更していない。
- 最終コードで全589テスト（失敗/エラー0）とdistZip成功。追加は港町の建築と静的な42枚の帆布表示のみ。
  通常ゲームの稼働プロセス、live install、main、保存データ、client-fabric、resourcepackは変更しない。
- 品質目標は継続。今回は市場の前景と建物への接続を改善した区切りであり、街全体を高水準と判定したものではない。
  次の主な監査対象は、集会所の大屋根と回廊が住宅と同じ造形に寄っている点。港町を代表する建物の
  輪郭と構造を、主動線から見た実景でさらに作り分ける。

### 第21反復 / 集会所を開いた港の大広間へ

- 主動線からの姿が、商館と同じ窓/漆喰の切妻を大きくした形に寄っていたため、集会所正面を再構成。
  小さな玄関切妻を、正面幅いっぱいの浅い銅屋根と6本の支持柱による回廊へ変更した。
- 左右の7幅の開口を食堂と回廊の間に設け、実際に同じ床で行き来できるようにした。
  中央5幅の通路、従来の大階段と踊り場、内部の卓/炉/3組の架構/吊り照明は維持。
- 前面の大きな漆喰の妻壁を開き、扇状の木組みと棟を支える柱を露出させた。住宅風の窓と鎧戸は
  正面から廃止する一方、奥側は残す。既存の赤い旗は開口上の横架材に取り付けたままで、浮かせない。
- 主屋根の左右に反り上がる軒先を追加し、側回廊の既存柱から連続した持ち送りで支持。
  屋根の張り出しを増やすために床の通路を狭めることはしない。
- `market-r21.png` で、二段の屋根と開いた正面回廊を確認。`gallery-r21.png` では新しい側面開口を通じて
  食堂の卓/炉/照明と回廊がつながって見える。どちらも通常ゲームとは独立したVanilla実景。
- 市場側では上部の木組みが影に沈んだため、その木組みを明るい剥皮材へ変更し、支えのある2灯を設けた。
  材質の明度差で木組みを読ませる調整であり、無関係な小物の増量はしない。
- 重要箇所は `HarborDistrictArchitecture.hallSeawardFront`。`grandHall` の外殻/主屋根の後に正面を構築し、
  既存の内部架構と家具を配置する。問題時は同関数と `HarborSceneTest` の回廊開口/支持柱/主階段検査から確認。
- 正面中央に従来からあった柵の切れ目は階段につながらず、基壇の端へ出てしまうため閉じた。
  実際の大階段の開口x8〜12は維持。内部5幅の通路と階段から大広間への到達は継続して検査する。
- `market-r22.png` では上部の木組みを明るくしすぎ、横材まで大きな紋章のように目立った。
  そのまま採用せず、横材を暗い木、柱/斜材を中間色の剥皮トウヒに調整。梁に吊るす2灯は残す。
- `outlook-r23.png` は大広間から正面を見た最終コードの実景。左右が開いた回廊と、正面の腰高の柵を確認。
  屋根や隣の建物を含む実際の眺めを記録し、見えていない海面や街路まで広く見渡せるとは評価しない。
- 最新コードで全589テスト（失敗/エラー0）とdistZip成功。追加した回廊開口、支持柱、主屋根の持ち送り、
  上部の灯具の取り付け、正面の柵、実階段の開口も検査した。通常ゲームのlive install/main/保存データは未変更。
- `market-r23.png` は最終の木材明度。下の横材が輪郭へ戻り、明るすぎた面を抑えて柱/斜材を残した。
- `arrival-r23.png` は最終コードの到着桟橋。市場の左右の建物の間に、二段の大屋根と開いた正面回廊が見える。
  撮影専用のサーバー/クライアントのみ停止し、通常プレイ環境は維持した。
- 品質目標は継続。主動線の建物の役割は分かれてきたが、受付前の古い厚い布屋根など、今回の薄い帆布と
  表現がそろっていない箇所が残る。街全体の景観品質をテスト成功だけで完了と判定しない。

### 第24反復 / 布屋根の厚みと支えを統一

- 受付前の2屋台と岸壁の2つの庇を、1m厚の羊毛ブロックから静的な薄い標準BLOCK_DISPLAYへ変更。
  岸壁は縞のある片流れ、屋台は中央の棟で合わさる切妻の布屋根とし、既存の三角帆布とは形を変えた。
- 岸壁の布は上階のバルコニー床より下から張り出す。以前の羊毛が床を置き換えていた部分は木の床を残し、
  バルコニー・入口・階段を維持する。前梁、支柱、張り出し腕は実ブロックで支持し、重複した下の灯具を除いた。
- `market-r24.png` は最初の実景。薄い屋台屋根に対して元の柱と側梁が太すぎたため、屋台の架構のみ
  細い木のフェンスへ変更した。住宅や岸壁の上階を支える柱は太いまま残し、構造の役割を区別する。
- `quay-r24.png` では岸壁に沿った庇の薄い断面と、その上に残した木のバルコニーを確認した。
  通常ゲームではなく、保存処理のない独立したVanilla撮影環境の写真である。
- `HarborDistrictArchitecture.waterfront` → `clothTile` → `HarborScene.Result.scenery` が布の生成と保持の流れ。
  この区画の布は200枚（既存42＋岸壁104＋屋台54）の静的表示。クライアントコード・独自テクスチャ・tick処理は追加しない。
  崩れた場合は同関数と `HarborSceneTest` の屋根の連続面、床、柱、灯具の取り付け検査から確認する。
- `market-r25.png` は屋台の細い支柱にした最終形。太い住宅/回廊の柱の間で屋台の脚が軽くなり、
  後ろの受付と左右の道が見える。布屋根の下にある梁の接続は残し、浮かせていない。
- `arrival-r25.png` は桟橋からの最終形。岸壁の縞の布、街路側の三角帆布、奥の小さな屋台の順に重なり、
  中央の受付・大階段・集会所は遮らない。左右の上階バルコニーも存続している。
- 最終コードで全589テスト（失敗/エラー0）とdistZip成功。中央と横断路、全施設の到達性、各階段・乗船路に
  加えて、バルコニー床、灯具の吊元、屋台の柱の接地、布の厚みと位置も検査した。
  main、通常ゲームの稼働プロセス/live install/保存データ、client-fabric、resourcepackは未変更。
- `overview-r25.png` は街全体の配置を監査する俯瞰写真（プレイヤー目線の代用ではない）。
  集会所、塔、造船小屋、市場の異なる輪郭は確認できた。一方で桟橋に広い平板な木床が残り、
  船の厚いブロック帆と街の薄い帆布にも差がある。これらは今回未修正の品質上の懸念として残す。
  目標は継続し、この布屋根の改善だけで街全体を完成と判定しない。

### 第26反復 / 船の帆・支索・船首を再構成

- 前回の俯瞰で残った厚い帆を、大型船の2枚の横帆、小型船の三角帆/前帆に分けて薄い標準表示へ変更。
  高さ方向に緩い膨らみを付け、隣接する面を同じ端点で接続する。船種、帆の方向、色の違いは維持。
- 大型船の舷側は前後へ上がる輪郭に変更し、水線付近に段差のある船板と縦の補強材を設けた。
  船首の小甲板には段差と実ブロックの支持があり、既存の3幅の乗船路と中央の甲板を維持する。
- 最初の回帰テストでは旧支索が船首の段を上書きして失敗した。支索の下端を船首の突き出し材と
  船尾屋根へ移して通路から分離し、テストを通した。歩けない階段を見た目だけ置く案は採用しない。
- `berth-r26.png` の実景では薄い膨らんだ帆を確認できた一方、フェンスの支索が空中の階段に見えた。
  3本の支索を細い連続した静的表示へ変更し、大型船の下の横材2本も手すり状から細い棒へ変更。
- `cutter-r26.png` は小型船の初期実景。三角形の長い一枚面で羊毛の模様が引き伸ばされたため、
  最長1mの面へ分割し、テクスチャの極端な横伸びを避ける検査を追加した。
- 処理は `HarborScene` → `HarborDistrictArchitecture.ship` / `fishingCutter` → `displayBlock` / `riggingStay`。
  問題時はこれらと `HarborSceneTest` の帆の位置/厚み/面幅、マスト、船首段、乗船路の検査から確認する。
  表示は全493個（街の布200＋船の布288＋支索/横材5）の静的な標準BLOCK_DISPLAYで、tick更新やclient modはない。
- `berth-r27.png` は支索と下の横材を細くした最終形。船首からマストへ線がつながり、帆の膨らみと
  上がった船首が同時に見える。フェンスが空中階段に見えていた初期案との比較を保存した。
- `cutter-r27.png` は最終形の小型船。前帆の縁に沿う細い支索と、面を分割した羊毛の模様を確認。
  旧い支索の階段状の輪郭と、極端な横伸びを解消した。甲板へ続く桟橋の開口も写真の手前に残る。
- `overview-r27.png` の裏側に見える小さな明点を調べ、曲率が変わる面の端に1.5cmの重なりを追加。
  `overview-r28.png` にも明点は残り、改善を証明できていないため、この裏側の見え方は未解決として残す。
  船と布の変更後に全589テスト（失敗/エラー0）の結果と更新されたdistZipを確認した。

### 撮影ウィンドウ / サブモニター固定

- ユーザーの指示により撮影用Minecraftはサブモニターで開く。メイン側の動画や通常ゲームのウィンドウは操作しない。
- `start-harbor-review-client.ps1` は非プライマリ画面が1台であることを確認して起動位置を指定し、
  `place-harbor-preview.ps1` が実際の可視ウィンドウをNOACTIVATEで配置して画面内への収まりを検査する。
  対象PIDは `harbor-review-client` のコマンドラインに限定。配置に失敗した場合は撮影用クライアントだけ停止する。
- `capture-harbor-preview.ps1` も撮影前にサブ側へ配置・検証する。サブ画面がない/複数ある場合は
  メインへフォールバックせずエラーとする。問題時は `place-harbor-preview.ps1 -ListMonitors` と配置ログを見る。
- 実機はメイン(0,0)-(1920,1080)、サブ(-1920,0)-(0,1080)。撮影用PID29364を
  (-1760,70)-(-160,1009)へ配置し、接続ログと `overview-r28.png` の実描画を確認した。

### 第29反復 / 桟橋の床下を実際の杭と梁へ

- 前回の俯瞰と岸壁の写真では、広い板床と連続した厚い側面が桟橋を平板に見せていた。
  板床を上付きハーフブロックに変更し、旧来の足元y41を保ったまま板の厚みを半分にした。
- 矩形ごとに無関係な位置へ置いていた床下の梁を廃止。到着桟橋の4組の杭、岸壁の杭、左右の
  乗船桟橋の対になった杭の位置へ床梁をそろえた。杭の間は空間を残し、水面が見える。
  梁の足元には水没する木の持ち送りを付け、中央の通路に新しい柱を置かない。
- 到着桟橋の縁に点々とあった低いハーフブロックを、分岐を避けてつながる手すりへ変更。
  海に突き当たる南端には柵を設けた。中央5幅の既存ルート（z37まで）と左右3幅の乗船路は維持する。
- `HarborDistrictArchitecture.deck` → `boardingPiers` → `pierFrames` → `connectDetails` で床と支持構造を組み立てる。
  問題時は `pierFrames` と `HarborSceneTest` の上付き床/床下の空間/杭/乗船路の検査を最初に確認する。
- `berth-r29.png` はサブモニターで撮影した岸壁の実景。従来の塞がった側面の下に水面が見え、
  杭と床梁の位置が揃った。船体と岸壁から桟橋への接続は維持されている。
- `arrival-r29.png` は到着桟橋からの最終コードの実景。左右の手すりが接続し、横梁が板床を区切る一方、
  中央の道と高台の集会所への見通しは維持した。手すりは横断する乗船路の開口に置いていない。
- 最終コードで全589テスト（失敗/エラー0）とdistZip成功。代表床の上付き指定と直下の空間、梁・杭の連続、
  既存5幅の主動線・3幅の乗船路・施設到達性を検査した。今回の建築で表示Entityは増やしていない。
  通常ゲームのlive install/main/保存データは変更せず、撮影用プレビューはサブモニター上に残した。
- 品質目標は継続。桟橋の支持構造は改善したが、街の外周の地形と建物の境界、船の帆の裏側の細かな明点など、
  街全体の最終評価に必要な未解決部分がある。テスト成功を街全体の完成判定には使わない。

### 第30反復 / 東の宿舎と岩場を石造テラスで接続

- `coast-r30.png` は外周の岩場から見た修正前の実景。前景の岩で下部が一部隠れるが、石の基壇が
  水平な帯に見え、建物と岩場の間に空間的なつながりが乏しい箇所を確認した。
- 東側の高台の海側へ、端が一段引っ込む石造テラスを追加。4組の控え壁を上の木柱の位置に合わせ、
  地面から床まで連続させた。基礎の低い部分は苔のある石、高い部分は石レンガ、頭部は安山岩とした。
- 元の床と足元の高さを維持し、海側に通れる幅を追加した。中央部分の2幅の通路が既存の高台階段から
  到達できることをBFSで検査し、海側の縁の柵と控え壁の連続も検査する。入口や元の階段は変更しない。
- 主な処理は `HarborDistrictArchitecture.build` → 岩場・建物・植生 → `cliffsideTerrace` → 接続形状の確定。
  問題時は `cliffsideTerrace` と `HarborSceneTest` の海側テラス到達性/縁/控え壁の検査を最初に確認する。
  撮影には既存地形上の2地点 `HarborCoast` / `HarborCoastSouth` を追加し、サブモニター固定で使用する。
- `coast-south-r31.png` は南東の岩場から見た新しいテラス。出っ張った控え壁とその間の陰、上の床と柵が
  建物の木柱へつながって見える。岩場の凹凸を覆い尽くす盛土はせず、石積みが岩へ入る部分を残した。
- `coast-r31.png` は修正前と同じ地点・角度。岩の前景はそのまま残り、その奥に新しい床の張り出しと
  支持の凹凸が見える。別角度の写真だけで差が出たと判断せず、同じ位置の比較も保存した。
- 最終コードで全589テスト（失敗/エラー0）とdistZip成功。新しいテラスに加え、主動線、全施設、
  高台の既存階段と集会所、桟橋と乗船路の既存検査も通した。通常ゲーム/main/live install/保存データは未変更。
- 品質目標は継続。今回改善したのは東側の高台と海岸の関係であり、他の裏側の建物や室内の使い分け、
  船の帆の裏側に残る明点などは継続して確認する。街全体の最終品質を今回の成功だけで判定しない。
- `overview-r31.png` は全体の配置監査用。東のテラスは手前の造船小屋と植生に一部隠れるため、
  改修そのものの証拠は近景2枚で判断した。撮影用の全景表示をサブモニター上に残している。

### 第32反復 / 宿舎の共用室・階段・寝室を接続

- 東の高台の建物を、入口から共用室、2幅の階段、上階の3つの寝室区画、バルコニーへ続く宿舎にした。
  外側の建物寸法と既存の高台階段は維持。前回の海側テラスへも共用室から出られる入口を設け、
  もとの窓台や張り出した窓枠が入口に残らないよう取り除いた。
- 共用室の暖炉は背面の壁を通る煙道へ接続する。卓と長椅子は階段横の通路を空けて配置し、
  上階は通路と窓側の寝室区画を分けた。寝台は足側/頭側の向きをそろえ、灯具は梁または戸棚で支持する。
  宿泊・復活・取引などのゲーム機能は追加していない。今回は建築と空間の接続だけ。
- `lodging-r32.png` は共用室の実景。梁から吊る灯具、暖炉、卓の間に通路が残る。
  中央の視点からは階段の側面が壁として見えるため、この写真だけを階段の歩行確認の代用にはしない。
- `lodging-upper-r32.png` は上階の実景。階段開口の手すり、寝室入口、梁の灯具、バルコニーへの
  出口が連続する。寝室の内部はこの方向から見えないため、別の近景でも確認する。
- 撮影用の初期名 `HarborLodgingUpper` は長すぎてログインパケットの送信に失敗した。
  `HarborRooms` へ改名し、起動スクリプトとプレビュー側の両方で名前の文字種と16文字上限を検査する。
  修正後はサーバーの接続ログと、実際の建築を表示したウィンドウを確認してから撮影した。
- 主な処理は `HarborScene` → `HarborDistrictArchitecture.build` → 既存の建物/テラス → `lodgingInterior`。
  問題時は同関数と `HarborSceneTest` の6段の支持/頭上空間/上階の踊り場/寝室・バルコニーのBFS検査を見る。
  階段を上る操作の最終的な感触はCreatorの手動確認事項であり、固定カメラ撮影から証明したとは扱わない。
- `lodging-bunk-r32.png` は北側の寝室の実景。足側と枕側がつながった寝台、窓、戸棚上の灯具が
  ひとつの空間として収まっている。外の岩場と海が窓に入り、背面の煙道は通路や寝台を貫通していない。
- `overview-r32.png` は全景の配置確認用。東の宿舎の煙突は屋根から立ち上がる小さな付属物に収まり、
  集会所と塔の主な輪郭は維持されている。室内の品質確認はこの俯瞰ではなく上記の近景3枚で行った。
- 最終コードで全589テスト（68スイート、失敗/エラー0）とdistZip成功。表示Entity数は493のまま。
  通常ゲームの25565/main/live install/保存データ、client-fabric、resourcepackは未変更。
  撮影用25575への接続と可視ウィンドウを検証し、全景を左のサブモニター内に残した。
- 街全体の品質目標はまだ継続。西側の汎用的な家並みの使い分けや植生の輪郭、船の裏帆の明点などは
  今回の宿舎改修では解決していない。全テスト成功や内装の追加を、街全体の完成判定に置き換えない。

### 第33反復 / 西の平屋を非対称屋根の帆仕立て工房へ

- `alley-before-r33.png` は変更前の西路地。左の平屋は住宅と同じ窓・小屋根を繰り返し、
  路地に面する壁も閉じていた。今回はその既存の平屋だけを作り直し、隣の倉庫と高台の家は維持する。
- `merchantHouse` の平屋呼び出しを専用の `sailmakersWorkshop` に置き換えた。短く急な北側と、
  軒下の作業空間へ長く下る南側を持つ屋根にする。銅は少し風化した色とし、住宅の赤い小屋根と区別する。
  3組の柱・梁・棟を支える束・垂木をつなぎ、装飾だけの新しい階や見せかけの窓を追加しない。
- 東の路地側に2つの大きな開口、北側に既存の高台階段方向へ続く入口を設けた。
  卓と織機は壁際/作業区画に置き、前面の軒下と東側の通り抜けを空ける。新しいクラフト機能は追加しない。
- 植生の後で既存平屋の範囲内を構築し、葉が屋根や室内に食い込まないようにする。
  クリア範囲は旧平屋の屋根と張り出しの範囲内で、北の階段や東の既存路地は含まない。
- 主な処理は `HarborScene` → `HarborDistrictArchitecture.build` → 植生 → `sailmakersWorkshop`。
  問題時は同関数と `HarborSceneTest` の工房各入口のBFS/柱の連続/棟の方向/屋根の段差/灯具支持の検査を見る。
- `alley-r33.png` は変更前と同じ位置・角度。左の屋根裏の窓と厚い小屋根がなくなり、作業室の床と
  北の入口が見える。右の倉庫、奥の鍛冶工房、路地の幅は維持している。
- `sailshop-r33.png` は最初の軒下の近景。高い北側の窓と奥の入口は確認できたが、水平梁の端が
  空間の途中で終わって見えたため、梁を少し下げて前後の実柱まで通すよう修正した。
- 最初の全テスト実行は、屋根の木製端部まで銅製と期待する古い検査が実行されて1件失敗した。
  該当端部の実ブロックと、テストソースの更新がコンパイルより後だったことを確認し、
  木の端部/銅の中間部をそれぞれ検査する最終ソースで再実行し、成功を確認した。
- `sailshop-r34.png` は梁修正後の同じ近景。途中に見えていた梁の切り口がなくなり、前後の柱を
  結ぶ通し材になった。北の入口と上の採光窓、路地へ抜ける大開口も維持した。
- 最終コードで全589テスト（失敗/エラー0）とdistZip成功。旧来の主動線・施設・高台階段・桟橋に加え、
  工房の各入口への到達、柱と通し梁の連続、急な側の階段屋根と緩い側のハーフ屋根を検査した。
  表示Entity数は493のまま。通常ゲーム/main/live install/保存データ/client-fabric/resourcepackは未変更。
- `alley-r34.png` は梁修正後の路地。横からも梁が柱までつながり、工房越しの北側入口と鍛冶工房への
  道は残る。近景は最終コードで2地点から撮影した。手動操作のfeel確認はCreatorが行う。
- `overview-r34.png` は全体配置の監査用。西の工房は手前の倉庫にかなり隠れるため、
  この写真だけでは非対称屋根の外観を評価できない。既存の西の岩棚に立つ `HarborWestShore` を追加し、
  視点の足元に実際の床があり、頭の位置が塞がっていないことも検査する。
- `west-shore-r34.png` は低い岩棚からの初期写真。手前の低木と岩が工房を隠したので、建築の評価には
  使えなかった。地形や植生を撮影都合で削らず、同じ岩棚の少し高い場所へ視点を変更して再確認する。
- `west-shore-r35.png` は高い岩棚からの近景。長く下がる軒と周囲の住宅との高さの違いを確認できたが、
  西壁が広い無地の面に見えた。柱・通し梁の間に高さを変えた2組の横長の採光窓を設け、
  室内の作業場所と海側の外観をつなぐ。窓台とまぐさは既存の壁内に収め、岩場へ張り出させない。
- `west-shore-r36.png` は窓を入れた最終外観。同じ岩棚から窓越しに室内が見え、木柱と横梁に沿って
  大きな壁面が分かれる。屋根の長い傾き、前面の柱付き開口、岩場と植生は維持した。
- 採光窓と撮影位置の修正後も全589テスト（失敗/エラー0）とdistZip成功。新しい窓は柱や通し梁を
  取り除いていないことを検査し、通常ゲーム25565の稼働プロセスは触っていない。
- 品質目標は継続。今回改善したのは西の平屋の構造と内外の見え方であり、西の高台の家の使い分け、
  植生の大きな塊、船の裏帆の明点などは未解決。街全体の完成と同一視しない。
- `sailshop-r36.png` は窓追加後の軒下。新しい西窓自体は手前の柱に隠れるが、通し梁、作業卓、北側の
  階段へ続く入口、東側の大開口が維持されたことを確認した。外側の窓の評価は `west-shore-r36.png` を使う。
  最終の接続ログと可視ウィンドウを確認し、撮影用の工房表示を左のサブモニター内に残した。

### 第37反復 / 街中のヤシを塊状の樹冠から個別の葉へ

- 前回の `west-shore-r36.png` では、ヤシの葉が大きな楕円の塊になり、幹の上部と建物の輪郭が
  隠れていた。街中の6本について、幹の曲がり・冠の高さ・個別の葉先を持つブロック構成へ変更する。
- 海側へ長い葉、建物側へ短い葉を伸ばし、葉の中ほどだけ幅を持たせ、端は1ブロック幅で下げる。
  木ごとに高さを変え、葉の間に空が見える隙間を残す。遠景の植生は今回変更していない。
- 幹の曲がり目は横方向の接続も埋め、面同士がつながるようにする。建築後に植える順序へ変更し、
  葉は空気または既存の葉にだけ置く。工房や集会所方向の葉を短くすることで、室内への侵入も避ける。
- 主な処理は `HarborScene` → 建築/テラス/内装 → `HarborDistrictArchitecture.planting`。
  問題時は同関数と `HarborSceneTest` の西の幹/葉先/葉間の空間/葉数上限/工房内への非侵入検査を見る。
  木の数、表示Entity数、施設や通路の位置は増やしていない。
- `west-shore-r37.png` は初期形の同位置比較。旧来の丸い葉の塊がなくなり、幹の曲がりと工房の屋根が
  分かれるようになった。ただし斜めに落ちる葉が離れたブロックに見えたため、葉の曲がり目にも面接続を補う。
  西の樹冠全体が6方向の隣接で中心へつながる検査を追加し、葉数上限と空の隙間の検査も維持する。

### 2026-09-07 / 参照3作品を基準にした街全体の一括改修（r38–39）

- ユーザーの最新指定は [建築リファレンスと作業制約](harbor-visual-reference.md)。
  小さな変更ごとのビルド・再撮影を止め、街全体の屋根と立面をまとめて変更した。
- 倉庫は大きな荷揚げ用切妻と吊り具、商館・高台西住宅・東宿は位置と幅を変えた側面切妻、
  集会所は主棟より低い東の交差屋根、学院は緑銅の主屋根と街路側の小屋根へ変更。
  窓の面を柱より奥に置き、張り出しは持ち送りと既存壁へ連続する床で支持した。
- 全主要建築の塗壁を白い方解石に統一。上階の床帯と外側の細い窓枠を追加した。
  下階の入口、商館・宿のバルコニー出入口、階段と既存施設座標は変更していない。
- 屋根は銅・煉瓦・暗赤色・緑銅の4系統。軒から棟への色変化を大きな段として設け、
  切り替わりだけを数ブロックずらした。独立ランダム置換ではない。
  太い木の縞は屋根面から外し、必要な構造材を屋根の下へ移した。
- 西の荷揚げ岸壁に、2本の支持脚・連続した持ち送り・梁・吊り鎖を持つ貨物クレーンを追加。
  中央到着動線や船の乗り込み位置を変えず、平たい岸壁に用途の読める高さを与えた。
- 前ターン未確定の6本のヤシの面接続も同じ一括変更に含める。専用の葉1ブロックごとの
  再撮影はせず、今回の全景と主要動線の写真で建物との関係を確認する。
- 最初の全テストは589件中1件失敗。商館の床帯が市場の帆を支える既存の柱を上書きしていた。
  外側の床帯・細枠・持ち送りは空気部分にのみ置く処理へ修正し、既存部材を保護した。
- 最終の `:server-minestom:test :server-minestom:distZip` は全589件成功、失敗/エラー0。
  新しい屋根の棟方向・窓の後退・持ち送り・材料7種・クレーンの支持と吊り鎖を検査。
  既存の全施設への到達、到着5幅、上階・高台・宿の階段、ガード、船の乗降検査も通過。
- 処理順は `HarborScene` → 既存の地区建築/内装 → `districtSilhouettes` → 植生/船/接続処理。
  問題時は `HarborDistrictArchitecture.districtSilhouettes` / `sideRoofBay` / `roofMaterial` と
  `HarborSceneTest` を先に見る。表示Entity数は493のままで、リソースパックやMODを追加していない。
- `overview-r38.png` / `market-r38.png` は一括変更の途中確認。
  白壁とクレーン、干渉修正まで含む最終状態は `*-r39.png` で区別する。
- 通常ゲームの25565、main、live install、保存形式、client-fabricは未変更。
  プレビュー25575だけを再起動し、撮影用Vanillaウィンドウは左のサブモニター内に限定。
- 最終画像4枚を実見：`arrival-r39.png` は低い市場越しに集会所が見える到着構図、
  `market-r39.png` は鍛冶場・学院・集会所の異なる形と開いた動線、`gallery-r39.png` は
  柱/屋根の支持・ランタンの吊り構造・居室との連続、`overview-r39.png` は街全体の屋根の違いと
  船/クレーン/地形の関係を確認できた。3つの地上視点と俯瞰は別の証拠として扱う。
- 写真用4アカウントの接続は `preview-20260907-090223.log` で確認。最後の
  `HarborArrival`（PID23848）をサブモニターに残し、通常サーバーPID30408は維持。
- 残作業の焦点：街外縁の岩台と遠景の急斜面が規則的で、参考港町の「地形と建築が一体」の
  品質にはまだ差がある。次は海岸・基壇・植生のつなぎを一括で扱う。
  確定した屋根/窓の微調整へ戻って再撮影を繰り返さない。現時点で目標全体の完成とはしない。

### 2026-09-07 / 海岸・基壇・樹林の一括統合（r40）

- `HarborBackdrop.coastalStone` を街の岩棚と岬で共用。水際は暗い岩、上は白い方解石と
  曲がった閃緑岩の地層にし、建築の白壁と南国の海を同じ風景へつないだ。
- 岩棚に高さと縦横比の違う6つの突き出しを追加。外周の範囲内に限定し、基壇の直線を
  部分的に隠す。内側の路地、海沿いテラス、船の接近路や桟橋へは広げていない。
- 背景は局所勾配で岩面と植生面を分ける。急斜面の頂部に土3段を重ねる旧処理を廃し、
  岩肌と緩い面の草・根付き土を使い分け、露出する茶色い等高線の反復を抑えた。
- 背後の岬に23地点の広葉樹を配置。幹、枝、重なる複数の葉群で樹林の中景を構成。
  既存のヤシは残し、曲がり目と葉の落ち際を面接続に統一した。
  背景植生も街の保護領域には描画せず、ゲーム用Entityは増やしていない。
- 変更は `HarborBackdrop` / `coastalRockAprons` と対応する `HarborSceneTest` のみ。
  家屋・屋根・窓・施設座標・階段・船・保存形式は今回変更していない。
- 新しい検査は実チャンクを生成し、岩層・樹冠・地面から生える幹、沿岸岩の支持、
  既存の西海岸視点の頭上空間を確認する。以前からの街全体の動線検査も実行する。
  形と画面の品質は別途 `*-r40.png` の実ゲーム画像で判断する。
- r40の初回ビルドは全589件成功。全景では白い海岸と樹林が確認できたが、裏手の実景は
  地面が茶色い階段状に見えた。これは大きな画面上の欠陥として一括修正の対象にした。
  集会所直後の勾配を緩め、斜面表面と直下を苔に変更、低木を高さの変わる群落へまとめた。
  r41を最終確認用とし、屋根や窓の再調整は行っていない。
- `coast-r40.png` は足元の岩と基壇が画面の多くを占め、沿岸全体の品質を判定するには弱い。
  地形を撮影都合で削らず、既存の `HarborCoastSouth` で海沿いの立面を確認する。
- r41の最終コードでも全589テスト（失敗/エラー0）とdistZip成功。
  `overview-r41.png` は街全体、`coastsouth-r41.png` は宿/基壇/海岸の接続、
  `rear-r41.png` は庭先から苔の斜面と樹林、`arrival-r41.png` は到着の構図を実見した。
  茶色い等高線状の斜面は解消し、既存の建築・入口・回廊に新しい閉塞は見られない。

## 完成時監査 / 2026-09-07

| 要求 | 確認した証拠・判定 |
| --- | --- |
| 参考港町をMinecraftで再構成 | `arrival-r41` / `overview-r41`。船・荷揚げ・市場・工房・倉庫・高台集会所を、海から奥へ重なる構図で統合。ネイティブブロックと静的Displayによる実ゲーム表示。 |
| 到着桟橋→市場→高台の区画 | `arrival-r41` / `market-r39` / `gallery-r39`。近景の桟橋/軒、中景の低い市場、背景の集会所が分かれ、内部まで連続する。r40–41では当該建物の形を変更していない。 |
| 地形・シルエット・密度・支持構造 | `overview-r41` / `coastsouth-r41` / `rear-r41`。白い岩、緩い庭先、樹林を段階的に配置。主要建物の棟方向・高さ・用途を分け、柱/基壇/持ち送りを実ブロックで接続。 |
| 既存機能・移動経路の維持 | 最終 `HarborSceneTest`。5施設、到着5幅、従来の施設接近路、階段/頭上空間/踊り場、商館と宿の上階・バルコニー、海沿いテラス、船への乗降、支持材・手すりを検査。全サーバーテスト589件成功。 |
| 基準区画を街全体へ展開 | r39–41の全景と既存内装写真。集会所・学院・倉庫・商館・住宅・帆工房・修船場・鍛冶場を同じ素材系統で統一し、同型コピーにはしていない。 |
| 指定3作品の建築品質を参照 | `harbor-visual-reference.md` の実見記録と現画像を比較。屋根の重なり、非対称な形、窓の後退、素材の段階変化、植生との一体化を確認。外観レビューはPASS。作品の寸法や撮影効果を複製したという意味ではない。 |
| 実画面と接続確認・サブモニター | `preview-20260907-091619.log` の4つのQA接続と、各写真の実ウィンドウ確認。表示範囲は左サブモニター `(-1760,70)-(-160,1009)`。通常ゲームの操作は行っていない。 |
| 建築だけ・mainを変えない | 実装は建築/背景/プレビュー/テスト/記録。通常25565、live install、経済・UI・職業・保存形式・client-fabric・リソースパックは変更していない。作業branchにcommit/push。 |

外観のPASSは上記の実景と指定リファレンスに基づく制作上の判断であり、Hypixel/Wynncraftによる
認定や、万人の好みが一致するという主張ではない。手動ゲーム操作のfeel判断はCreatorに残す。
要求した港町の全体構成と建築の実装・検証は完了として閉じ、未指定の小物や微修正を追加し続けない。

重要な実装: `HarborScene` → `HarborDistrictArchitecture` → `HarborBackdrop`。
壊れた場合は `HarborSceneTest` とプレビュー接続ログを先に見る。配布物は
`server-minestom/build/distributions/server-minestom-0.1.0-SNAPSHOT.zip`。本編へのmerge/restartは別途指示時に行う。
