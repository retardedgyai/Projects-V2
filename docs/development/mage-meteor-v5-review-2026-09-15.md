# 流星雨 v5 — 参照に基づく再制作の途中 checkpoint

Verdict: **FIX-FIRST**。全10技の品質目標は継続中。流星も完成扱いにしない。

## この checkpoint の変更

- `scripts/build_mage_meteor.py`：岩体・着弾・地面の余波を専用の3クリップに分離。
- 岩は一つの欠けた面取り体積。表皮のUVを細かな面ごとにリセットせず連続させる。
- 新しい原画 `assets/combat-vfx/mage-v5/sources/meteor-basalt-v01.png` を無加工でRPへ配置。
  built-in image_gen使用。正確なプロンプトは同フォルダーの `prompts.md`。
  SHA256: `41ccdd9974f44a85d8ab27e3824033f60c5dc73a6dcdaf205dc88852c376424d`。
- 下側の明るい割れた面を追加。岩は222 elements（上限1000は変更なし）。
- 接触後は短い明面→独立して飛ぶ小片→細い余波。旧fire_chargeを流星の予告から除外。
- `CoreMageChoreography.kt`：初回PREPAREの実際の長さに合わせ、最後の岩を打撃直前まで残す。
  追加の空フレームが挟まる問題を、実際のItemDisplay出力を使う回帰テストで確認。
- Damage / Hit / 入力 / CD / MP / モブ / Warrior / UI / クライアント処理は変更していない。
  同じ`meteor`アセットを使う天火の予告の岩も外観は変わるが、天火全体の再制作は未完了。

## 実表示に近い検証と限界

`CoreCombatMeshTest`がサーバーの実表示メタデータを `.tools/mage-material-timeline.json` へ出力。
3回とも、tick11/19/27に最後の岩、tick12/20/28に新しい着弾を確認。
途中の調整ではtick11が空になっており、pose単体のテストでは見逃したため、出力列の回帰検証を追加した。

`.tools/meteor-v5-final-eye.gif` は、そのモデル・UV・姿勢を20Hzで投影したQA用動画。
ゲーム画面ではなく、Minecraftの照明・背景・ネットワーク・手触りの合格証拠ではない。
側面確認は `.tools/meteor-v5-final-side-{11,12,15,27,39}.png`。
真横からは現状の接触が薄すぎてほぼ見えない。後述の追加参照とも合わせ、爆風の立体的な主形が必要。

## 参照レビュー

R12（Wynncraft Meteor）の暗い岩と熱い割れ目、R09（MatE Solar）の明部・暖色層・暗部を参照。
補助レビューも実モデルのSSを確認した。

改善した点:
- 炎片の寄せ集めではなく、落下する岩として読める体積になった。
- 落下の本体と接触後の形が別の役割になった。
- モデルの輪郭が暗い背景に埋まる問題へ、落下先側の熱で対応した。

未達:
1. 明るい下端が均一な発光キャップに見える。2〜3か所で熱が側面へ食い上がる、不均一な割れ方が必要。
2. **新たな0.25倍の動画確認で、長い燃焼の尾と立体的な着弾爆風を確認した。現在は両方不足。**
   155.55秒の尾→155.70秒の明るい接触→155.84秒の弓なりに砕ける爆風を次の直接資料にする。
3. 複数落下のリズムと、他9技との一貫性はまだ視覚的に合格判定していない。

`.tools/wynn-meteor-slow-review.png` は同じR12の追加位相。31件を水増ししない。
「床の輪を追加したから参照水準へ達した」とは判断しない。

## Test / build

- 現worktreeのGradle対象4 suite: 57 tests、失敗0。最後の表示列回帰テストを含む。
- 現worktreeの流星専用Python: 3 tests成功。
- pack verifier: 16228 assets成功。未採用v4の他技を含むため、見た目の合格を意味しない。
- **commit対象だけをindexから独立した一時ツリーへ取り出して検証**：
  既存の全Mage材質テスト3件＋新しい流星テスト3件が成功。
  元のworktreeにある未採用v4の変更は保持し、このcheckpointへ混ぜていない。
- 現worktreeの全Python suiteは未合格。旧v4 `pyre` frame0に1446 elementsという上限超過が残る。
  また途中の一回の全suite実行は、ロード済みの旧生成関数と再生成後モデルが混ざったため再現比較に失敗した。
  その実行を現在の流星の証拠にせず、生成完了後の専用テストと独立ツリーで再検証した。

## 続行時

まずR12の追加動画を再確認し、尾・熱い縦の裂け・立体的に破裂する爆風を一つの発動として構成する。
テクスチャ変更だけで済ませない。他9技の不足は参照台帳の対応表を維持し、必要な動作を追加観察する。
ゲームは起動していない。main・共有core・Warriorの承認済み作画は変更していない。

最重要ファイルは `scripts/build_mage_meteor.py` と `CoreMageChoreography.kt`。
壊れた時は専用Pythonテスト→表示列回帰テスト→実メタデータのプレビューの順で確認する。
