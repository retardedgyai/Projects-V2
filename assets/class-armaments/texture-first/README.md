# テクスチャ先行の武装原稿 — 未採用

基準: [MatE / The Dark Sister](https://x.com/MatE312001/status/2096478906299699459)

## 最新状態 — Python加工の許可後

### 刃の余韻の側面欠けを修正（2026-09-09）

動く赤い破片の側面を「全フレームの塗りの合成外周」だけから作っていたため、
破片が合成シルエットの内側を通るフレームに必要な側面がなかった。
`compile_model(..., boundary_frames=...)` で各コマの外周を先に求め、その和集合から薄い側面を作る。
前後の絵・本体・鍔・宝石・握り・24コマの色/動き・厚さ0.20は変えない。
ブロックを増やす修正ではなく、動くテクスチャの輪郭に必要な面を補う。

- v02の旧出力では各フレームに3〜11個の境界画素が欠けていた。修正後は24コマすべて0。
  `test_animated_silhouette.py` は移動する1画素の最小再現と、両方の実原稿の全コマを検査する。
- 効果部分155→197要素、本体等を含むv02全体417→459要素。各要素は1〜2面のみ。
  通常の静止画コンパイルは既定値のまま、アニメーション利用箇所だけ明示的に有効化する。
- 隔離プレビュー2種とT1大剣だけの手動テストsnapshotを再生成。インストール済みJAR/RPは未変更。
- 関連Python38件成功。実Vanilla 26.2でsnapshot内725武器モデル（今回の25状態を含む）を読み込み、
  不正な回転軸の対照試験は拒否。24コマのネイティブmetadataも成功。GPU/実ゲームの最終表示は別途。
- 最重要/不具合時の確認先: `scripts/build_texture_first_sword.py` の `compile_model` と
  `scripts/build_blade_ember_study.py` の `animated_model`。基準の絵の承認や全武器完成を意味しない。

絶対零界の床/帯/結晶VFXは別checkpoint `faffb8c` で実装済み。
詳細は [frost domain](../../combat-vfx/frost-domain-v1.md)。以下の再確認記録はその実装前の時点。

### ほかの指定作品の再確認（2026-09-09）

ログイン後に [Ice Storm](../../../docs/research/ice-storm-direct-reference-2026-09-09.md) と
[Blighted Mace](../../../docs/research/blighted-mace-2026-09-09.md) の指定X版を直接確認した。
Ice Stormは武器展示ではなく、床の渦・上方の角張った帯・雪の結晶を重ねた範囲VFX。
現在の氷スキルのコードと比較し、次の改善対象を `mage_zero` に絞った。
メイスは柄まで続く青緑の縁と離れた小片を追加の照合条件にした。
今回この二つの実装変更はまだ行っていない。動画の正確な周期/元メッシュは未確定。
SamusDev動画の確認数は増えていない。防具制作は中止のまま。

### 大剣v02を手動テストへつなぐ専用起動モード（2026-09-09）

`scripts/build_material_playtest_pack.py` でインストール済みサーバーJARのRPを読み、
**T1大剣のアイテム参照一つだけ**を本体v02/鍔の奥行き/赤い刃のエフェクトへ変更する確認用snapshotを追加。
元JAR・通常RPを変更しない。ほかの武器/Tier・防具・UI・装備性能はそのまま。

次にユーザーが起動を求めたときは、snapshotを生成してから
`scripts/start-core-loop.ps1 -WeaponMaterialReview -JavaHome <Java25>` で選択できる。
通常起動では選ばれない。全武器を変える既存の `-WeaponArtReview` とは同時指定不可。
今回この起動コマンドは実行していない。ユーザーのゲーム・サーバーは操作していない。

- 原稿再変換 → 薄い本体/独立鍔/宝石 → 25ポーズと24コマの刃テクスチャ →
  JAR内RPに一つの装備参照だけ差し替え → 専用classpathから次回起動時に配布。
- `projects:weapons/greatsword_t1` を維持し、既存サーバーのCustomModelDataの
  0〜11待機/12〜17構え/18〜23放出/24以上静止のRPグラフを接続する。
  ステータス、ダメージ、サーバーのクラス共通処理、クライアントコードは変更しない。
- 対象Python35件、起動mock8件成功。ポート使用中/古いJARのsnapshot/両モード同時指定は起動前に拒否。
  実26.2の25モデルと連番metadataを検査。実サーバーのbundle関数で8,510ファイルをbyte単位で照合。
  ゲーム内の表示/握り/最終feelは未確認。参考同等の作画認定はしていない。
- 最重要/不具合時の確認先: `build_material_playtest_pack.py` の `candidate_resources` / `assemble`。
  出力は `.tools/material-playtest-resources`。古いsnapshotで起動拒否されたらbuilderを再実行する。
  この確認用パックは全武器の完成や本編採用を意味しない。防具中止の方針も変わらない。

### 指定作品を直接参照した大剣本体v02（2026-09-09）

ログイン済みのMatE指定ポストの正面寄りフレームを、組み込みimagegenに直接渡して
[本体原稿v02](sources/greatsword-material-v02.png) を生成した。
過去の自作原稿は生成のスタイル参照に渡していない。
[全文プロンプト・生成記録](generation-material-v12.json)。CLI/APIは使用していない。

- 以前の紫黒い素材に比べ、灰色の鍔・柄と赤い刃の色面が区別しやすくなった。
  元PNGはRGBの描かれたチェック背景だったため、許可済みの専用抽出/中央値ピクセル化を行った。
  本体80画素高/32×128キャンバス。旧16色パレットへは押し込まず、原稿の色面を維持する。
  生成原稿内の細かな色差も残るため、色数の多さを品質向上の根拠にはしない。
- `scripts/process_sword_material_redraw.py` が原稿SHA固定、抽出、宝石/受け皿分離、
  鍔の実際の灰色部分の輪郭分離を行う。左右の薄い鍔は±22度の別平面にし、
  刃と握りは固定。鍔の空隙をブロックで埋めない。
- `scripts/build_blade_ember_study.py --redraw` で新しい輪郭専用の5か所から赤い形を出す。
  既存v01と別の `.tools/blade-ember-redraw` に出力する。
  同ディレクトリの `blade-motion.gif` / `blade-motion-frames.png` は保存JSON/PNGの確認用正投影。
- 原稿 → processed-material-v02 → 薄い本体/独立した鍔と宝石 → 連番エフェクト → 独立確認用RP。
  最重要/不具合時の確認先は上記processorの `convert` / `guard_depth`。
  赤い形がずれる場合はmanifestの `ember_emitters` とbuilderの座標変換を確認する。
- 対象Python31件、実26.2モデル1件と24コマのmetadata検査が成功。
  実機表示は未確認。参考と完全に同じ作画/動きの認定はしない。
  他の武器やTier、通常RP、起動用review pack、稼働中ゲームは未変更。防具は中止のまま。

### 大剣一本の刃エフェクト試作（2026-09-09）

`scripts/build_blade_ember_study.py` で、刃の縁に伸びる角張った赤い形と、
離れて小さく暗くなって消える破片を追加。刃先にも独立した短い形を置いた。
固定模様/宝石の動きだけでは不足していたための試作で、本体の作画自体はまだ従来品。
参考同等の質感として承認したものではなく、他の武器/Tierへの展開もしない。

- 原稿の刃の座標 → 24コマの透過ピクセル列 → 刃のすぐ外側の薄い面 → ネイティブ連番テクスチャ。
  `animation` metadataで動かし、クライアント改造やサーバーの点パーティクルは使用しない。
  色と形を段階的に変え、補間/ぼかしは使わない。周期1.2秒は参考から計測した数値ではない。
- 既存の本体/宝石PNG、モデルの本体部分、握り位置とdisplayは維持。
  独立した `projects:weapons/blade_ember_study` のみ書き出し、既存アイテム参照は上書きしない。
- `.tools/blade-ember-study/blade-motion.gif` と `blade-motion-frames.png` で正面/斜め/真横を確認。
  保存JSON/PNG/metadataからの正投影であり、ゲーム映像ではない。
  真横では薄い形が細線になる。本体の色面・金属の明暗・全体の輪郭の再調整は残っている。
- 対象Python27件成功。実26.2のCuboidModelで1モデル、AnimationMetadataSectionで
  64×128/24コマ/各1tick/補間なしを確認。GPU表示/実機の手元/他プレイヤーからの見え方は未確認。
- 不具合時は上記スクリプトの `EMITTERS`、`effect_frames`、`animated_model` を確認。
  全て`.tools/blade-ember-study`へ生成。通常RP・起動用review pack・稼働中ゲームは未変更。

### 最優先の修正 — 質感と刃の赤いアニメーション（2026-09-09）

ユーザーから「質感が違う」と指摘されたため、下記のTier別原稿は品質承認済みとして扱わない。
同じ作画での種類追加を止め、まず大剣一本を指定のMatE作品に直接照合する。
防具制作は中止のまま。既存の原稿・モデルは実験として保存し、稼働中のゲームへ適用しない。

ログイン後の指定ポストで回転中の複数フレームを確認。
赤い刃の輪郭の外に、赤〜橙/紫紅の四角い破片や細長い形が見え、フレームによって配置・形が異なる。
ユーザーが特に好むこのアニメーションを必須の確認項目とする。
固定の赤い模様や宝石の移動だけでは代用しない。
参考は大きなピクセルの色面も使っているため、単純な高解像度化・色数増加・ノイズ追加を解決策にしない。

[観察と次の一本の受け入れ条件](../../../docs/research/dark-sister-material-motion-2026-09-09.md)。
これは方向修正の記録であり、赤い刃のエフェクト実装完了の報告ではない。

### 弓T1〜T4の別原稿と矢つがえ（2026-09-09）

T1を維持し、T2の細い反り、T3の広い葉状の面、T4の外向きの角状の枝を追加した。
組み込みimagegenで [T2](sources/bow-t2-v01.png)、[T3](sources/bow-t3-v01.png)、
[T4](sources/bow-t4-v02.png) を生成し、元PNGを保存している。
[全文プロンプト/生成記録](generation-bow-tiers-v11.json)。CLI/APIは使用していない。
T4初稿がT1に近すぎたため、外側の枝の輪郭だけを1回修正した。

- 許可済みPython加工でRGBチェック背景を除去、縦92画素/既存弓16色へ変換。
  原稿に弦/矢を焼き込まず、上下の弓身と握りを原稿の留め具位置で分離する。
- 原稿 → 分離PNG → 弓先端/根元 → 引き絞り → 弦端/矢のつがえ位置 → 25ポーズ → 確認用パック。
  固定の握りから上下の弓身が動き、弦は動く先端とつがえ位置を結ぶ。
  矢は既存の独自原稿を共用し、構え末尾と放出先頭を一致させ、前へ進んでから非表示にする。
  手持ちの化粧モデルであって、サーバー上の発射体や当たり判定を置き換えない。
- 最重要ファイル/不具合時の最初の確認先は `scripts/build_bow_tiers.py` のJOBSとcuts。
  弦が外れる場合は `build_specialist_armament_pack.py` の `bow_points` と先端/根元を確認する。
  既存の弓のアニメーション関数やT1の出力は変更していない。
- `.tools/bow-tier-pack/tiers-review.png` で正面/最大引き絞り/放出途中の斜めを確認。
  T3の面の広さとT4の外向きの枝は読める。T2はT1と同系統で、意匠の差はまだ弱い。
  保存JSON/PNGの正投影で、ゲームの腕/カメラ込みの実機映像ではない。

対象Python77件、起動mock4件、実26.2の550モデル/2,200握り位置/400弓方向検査が成功。
全Tierの弦の端/つがえ位置の接続、固定握り、矢の前進/消失、75個の保存ポーズを追加検証。
実サーバーbundle関数の9,168ファイル/28武器参照も確認（実サーバー起動なし）。
全repoテストではない。モデル数/緑のテストは作画品質の承認を意味しない。
**防具制作は中止。通常RP・稼働中ゲーム・UI・性能は未変更。**
Tier別原稿は大剣/短剣/魔杖/メイス/弓まで。魔導書/星盤のTier別原稿は未完了。
全動画確認、全武器の参考同等の作画、実機確認も未完了。
動画158件中19件は部分観察、139件が未確認。全編視聴完了は数えていない。
[Archerの観察](../../../docs/research/samusdev-class-phrases-2026-09-09.md) を追加したが、
参考のスキルVFXをこの武器バッチで実装したものとはしていない。

### メイスT1〜T4の別原稿化（2026-09-09）

T1を維持して、T2の縦長の鎚頭、T3の横に張り出す鎚頭、T4の冠状の刃を追加。
組み込みimagegenで作った [T2](sources/mace-t2-v01.png)、[T3](sources/mace-t3-v01.png)、
[T4](sources/mace-t4-v01.png) は元PNGのまま保存。
[全文プロンプト/生成記録](generation-mace-tiers-v10.json)。CLI/APIは使用していない。

RGBチェック背景は許可済みPythonの暗色/有彩色抽出で除去し、縦84画素・既存メイス16色へ変換。
1画素あたり30/92モデル単位を維持。解像度や色数の増加でTierを表現していない。
原稿 → body/crystalの分離 → 原稿ごとの頭部/握り位置 → 交差面 → 各25ポーズ → 確認用パック。
結晶は構えで35度回り手前へ0.5移動、放出先頭は構え末尾と同じ、放出末尾は元へ戻る。
頭部/柄は固定し、+45/-45度の0.18厚の面が塗られた輪郭だけを構成する。

- 最重要ファイル/不具合時の最初の確認先は `scripts/build_mace_tiers.py` のJOBS。
  結晶範囲、頭部の終端、握り位置を元画像から指定し、manifestへ投影する。
- `build_specialist_armament_pack.py` は原稿のレイヤーと頭部終端を受け取れるようにした。
  既存T1の終端35と握り範囲は維持。弓/本/星盤の処理は変更していない。
- `.tools/mace-tier-pack/tiers-review.png` で正面/構え斜め/真横を確認した。
  T2は細身、T3が最も広い。T4は冠状であり、Tier順に大型化する設計ではない。
  T4は結晶の上下に細い支えが残り、完全に浮いた原稿意図とは差がある。
  真横でも頭部の輪郭は残るが柄は細い。独立した背面原稿ではなく交差面に同じ絵を使用。
  この画像は保存JSON/PNGの正投影であり、実機映像や参考同等の認定ではない。

対象Python71件、起動mock4件、実26.2の475モデル/1,900握り位置/100弓方向検査が成功。
実サーバーbundle関数の9,060ファイル/28武器参照を確認（実サーバー起動なし）。
全repoテストではない。新原稿の実機の腕/カメラ込みの表示と最終品質は未確認。
**防具制作は中止。通常RP・稼働中ゲーム・UI・性能は未変更。**
別原稿は大剣/短剣/魔杖/メイス。残りは弓/魔導書/星盤のTier別原稿。
全動画確認、全武器の参考同等の品質、実機確認も引き続き未完了。
動画カタログ158件中18件が部分観察、140件が未確認。全編視聴完了は数えていない。
[Warriorの追加観察](../../../docs/research/samusdev-class-phrases-2026-09-09.md) は
防御の赤い殻/黄白い縁の斬撃面についてのメモで、このバッチのスキル追加ではない。

### 魔杖T1〜T4の頭部と開閉（2026-09-09）

T1を維持し、T2の細い叉、T3の幅広い翼、T4の開いた三日月を別原稿として追加した。
原稿は [T2](sources/staff-t2-v01.png)、[T3](sources/staff-t3-v01.png)、
[T4](sources/staff-t4-v02.png)。組み込みimagegenを使用し、CLI/APIは使用していない。
[全文プロンプト/生成記録](generation-staff-tiers-v09.json) に入力の役割と不採用T4も記録。
T4の初稿はT1に似すぎており、離れた破片もあったため、頭部だけを1回描き直した。

- 許可済みPython加工でRGBチェック背景を除き、既存のシアン12色/縦92画素へ変換。
  元PNGは保存し、結晶の黒い輪郭を含めてbody/jewelを完全に分離する。
- `build_staff_tiers.py` のJOBSで原稿ごとの首元/柄/結晶範囲/左右分割を定義する。
  `build_pixel_armament_pack.py` の `staff_fins` が、それぞれの塗られた輪郭から
  薄い装飾を作り、原稿上の付け根で開く。首元と柄は固定。箱は積み重ねない。
- 構え末尾と放出先頭は同じポーズ、放出末尾は元へ復帰する。結晶は上/手前へ出て回る。
  原稿 → 分離PNG → 支点付き薄板 → 各25ポーズ → opt-in確認パック、という流れ。
- `.tools/staff-tier-pack/tiers-review.png` の正面/構え/背面を確認した。
  保存済みJSON/PNGの正投影で、Minecraftの手持ち映像ではない。
  T3とT4は幅と空間の形が違うが同系統の意匠。参考同等の品質認定とはしない。

対象Python66件、起動mock4件、実26.2の400モデル/1,600握り位置/100弓方向検査が成功。
実サーバーbundle関数の8,970ファイル/28武器参照も確認した（プロセス起動なし）。
全repoテストではない。実機の腕/カメラを含めた見た目の検証は未実施。
重要ファイルと不具合時の最初の調査先は `build_staff_tiers.py` のJOBSと分離manifest。
別Tierが出る場合は `build_weapon_playtest_pack.py` のselected_keyを確認する。

**防具制作は中止、通常RP・起動中ゲーム・UI・性能は未変更。**
別Tier原稿が揃ったのは大剣/短剣/魔杖。残る弓/メイス/魔導書/星盤のTier別原稿、
全動画確認、全武器の参考同等の品質、実機の最終確認は未完了。
動画カタログは158件中17件が部分観察、141件が未確認。全編視聴完了として数えていない。
今回のMageの部分観察は [職業映像メモ](../../../docs/research/samusdev-class-phrases-2026-09-09.md)。

### メイス頭部の立体構成（2026-09-09）

元のメイスは頭部も柄も1枚の薄い正面形状で、真横から頭部が線になっていた。
`build_specialist_armament_pack.py` の `mace_body` で、v02原稿の頭部（行35より上）を
Y軸+45度/-45度の薄板へ分け、4方向へ張り出す形状にした。
板厚は0.18、塗られた画素だけの矩形面と輪郭の側面を使う。箱の積み重ねではない。
首元/柄は固定し、中央の結晶の既存アニメーションを維持する。
PNG/色/画素密度/原稿は一切変更せず、新しい画像生成も行っていない。

- 正面画素のmaskをtraceしているため、頭部へ柄が重複描画されない。
  2枚の頭部には同じ頭部の絵を使うが、柄は1回だけ配置する。
- `preview_mace_head.py` は以前の1面構成と保存済みの新モデルを同じ条件で比較する。
  `.tools/specialist-armament-pack/mace-head-comparison.png` の0/45/90/135度で確認した。
  これはJSON/PNGの正投影であり、実機映像ではない。柄の横方向の細さは今回未変更。
- 独自原稿の立体化であり、参考の正確なメッシュ/原画や白い発光輪郭の再現ではない。
  [参考の追加観察](../../../docs/research/blighted-mace-2026-09-09.md) と区別する。

処理: v02のbody画素 → 頭部/柄mask → 薄い交差面 → 従来の25ポーズ → 任意確認パック。
重要ファイル/最初の調査先は `mace_body` の行35境界・Y軸回転・trace_painted_faces。
テストは各板の全不透明画素の一致/重複なし、横方向の幅、柄/頭部の固定を追加した。
対象Python61件、起動mock4件、26.2の325モデル/1,300握り位置/100弓方向検査が成功。
実サーバーbundle関数の8,880ファイル/28武器参照も検証。通常RPと稼働中ゲームは未変更。
防具の中止は維持。全動画確認・残り5系統のTier別作画・参考同等の品質は未達成。

### 短剣T1〜T4の別原稿化（2026-09-09）

T1を維持し、T2鉤刃/T3幅広い反り刃/T4直線的な刺突刃を追加。
組み込みimagegenで原稿ごとに生成し、CLI/APIは使用していない。
原稿は [T2](sources/dagger-t2-v01.png)、[T3](sources/dagger-t3-v01.png)、
[T4](sources/dagger-t4-v01.png)。参考入力の役割と全文プロンプトは
[generation-dagger-tiers-v08.json](generation-dagger-tiers-v08.json)。
RGBチェック背景は許可済みPythonのforeground抽出で除き、縦60画素、既存の赤系16色へ変換。
大剣と同じ1画素=30/92モデル単位に揃え、短剣を高密度/高解像にしていない。
薄い刃面と画素境界の側面、分離した結晶、柄の必要な厚みという構成を維持。

- `build_dagger_tiers.py`: 原稿/SHA・鍔/柄の境界・結晶範囲 → 分離PNG → 各25ポーズ。
  赤い原稿の変換だけは大剣の `convert_original` を共用。大剣の既存出力も再検査した。
- `build_weapon_playtest_pack.py`: 既存 `dagger_t1`〜`dagger_t4` をそれぞれのモデルに接続。
  武器性能、装備ID、保存形式、スキル判定、クライアント処理は未変更。
- `.tools/dagger-tier-pack/tiers-review.png` は保存JSON/PNGの正面/斜め/背面の正投影。
  T2の鉤とT4の直線は明確。T3は純粋な三日月形にはならずT1に近い反り刃で、
  幅と鍔が主な差。縮小で細かな鍔の切り欠きが弱くなる。品質承認扱いにしない。

対象Python58件＋起動mock4件、26.2の325モデル/1,300握り位置/100弓方向検査が成功。
サーバーbundle関数で8,880ファイル/28武器参照を確認。通常RPと稼働中ゲームは未変更。
防具は中止を維持。UI/フォント/HUD等の既存資産は28武器item定義以外バイト同一。
重要ファイル/最初の調査先は `build_dagger_tiers.py` のJOBS、分離結果のmanifest、
誤Tier選択なら `build_weapon_playtest_pack.py` のselected_key。
**別原稿は大剣/短剣のみ。他5系統のTier別原稿、全動画確認、参考同等の品質は未完。**
今回の参考確認は [短剣とAwakened Assassinの部分観察](../../../docs/research/dagger-assassin-2026-09-09.md)。

### 弓の矢つがえ/放出（2026-09-09）

imagegen組み込みで [矢の原稿](sources/bow-arrow-v01.png) を追加。
入力は弓v02の材質/画素構成の参考であり、弓そのものの編集ではない。
[全文プロンプトと生成記録](generation-bow-arrow-v07.json) を保存し、CLI/APIは使用しない。
RGBチェック背景を許可済みPython加工で除去し、8×52の原稿画素/12色へ変換、
横向きの64×16 atlasとして `processed-bow-arrow-v01/arrow.png` に保存。
弓と同じ1画素=30/92モデル単位、最大厚0.16の薄い面。新しい立方体アセットは使わない。

- 構え6ポーズ: 矢の後端が変形中の弦中央に追従。矢の形/UVは伸縮しない。
- 放出: 最初は構え末尾と同じ。次の2ポーズで+Xへ5.5/12単位進み、その後は非表示。
  弦の復帰に合わせて矢が手元へ逆戻りすることを防ぐ。待機/GUIに矢は出ない。
- 弓だけ左右の表示回転を専用化。原稿+X（鏃側）が左右とも手持ち座標の-Zを向く。
  他の6武器は従来の回転を維持。従来の「左右同一Euler」の説明は弓には適用しない。
- 既存のfloat0/射撃通知を利用。`CoreArmamentPresentation.usesWeapon` の弓は
  ARROW/RAINのみで、罠・回避等は除外されていることをコードで再確認した。
  サーバーKotlin/攻撃判定/飛翔体を変更せず、手元の見た目だけを追加した。

処理の流れ: `process_bow_arrow_art.py` の原稿変換 →
`build_specialist_armament_pack.py` の薄い面/arrow_offset → 25ポーズ → 任意確認パック。
初動調査も同ファイル: 原稿/背景は変換側、逆戻り/タイミングはarrow_offset、
向きは `pixel_weapon_display.py` のbow専用回転を見る。
`.tools/specialist-armament-pack/bow-sequence.png` / `actions.gif` は保存モデルの正投影確認。
**実機のカメラ/腕の追加回転、実射撃との体感上の同期、参考と同等の作画は未確認。**
手元から消えた矢をワールド内で追跡する表現まで追加したものではない。

対象Python54件と起動mock4件が成功。実際の26.2で250モデル/1,000握り位置に加えて
100件の弓の手持ち座標内の向きを検証。実サーバーbundle関数で8,790ファイル/28参照も成功。
通常RP・稼働中ゲーム・防具・UIは未変更。全repoのテスト成功とは扱わない。
参考の追加観察は [Phantom Bowの部分確認](../../../docs/research/phantom-bow-arrow-2026-09-09.md)。

### 大剣T1〜T4の別原稿化（2026-09-09）

**防具は中止したまま。既存防具を削除/変更せず、通常RPと稼働中ゲームにも未反映。**

T1の既存原稿を残し、T2〜T4へ別の描き起こしを追加した。
T2は刃の大きな切り欠きと非対称の鍔、T3は棘と翼状の鍔、T4は冠状の鍔。
色替えではなく不透明画素の輪郭を変え、全段階を同じ縦92画素/物理高30/既存16色で揃えた。
薄い刃面と画素境界の側面で構成し、最大2面/要素を維持。厚い立方体の積み重ねはしない。

- 組み込みimagegenで1原稿1呼び出し。入力画像の役割と全文プロンプトは
  [generation-greatsword-tiers-v06.json](generation-greatsword-tiers-v06.json)。CLI/API経路は未使用。
- 原稿: [T2](sources/greatsword-t2-v01.png)、[T3](sources/greatsword-t3-v01.png)、
  [T4](sources/greatsword-t4-v01.png)。RGBチェック背景を許可済みPython加工で除去し、
  `processed-greatsword-tiers-v01/` に二値alphaの本体/結晶を保存。元画像も保存する。
- `scripts/build_greatsword_tiers.py`: 原稿/SHA → 画素化/分離 → 薄い形状 → 各25ポーズ。
  `build_weapon_playtest_pack.py` が既存 `greatsword_t1`〜`greatsword_t4` の装備IDを
  各原稿のモデルへ割り当てる。性能・保存形式・クライアント処理は変更しない。
- `.tools/greatsword-tier-pack/tiers-review.png` は保存済みJSON/PNGの正面/斜め/裏面の
  正投影確認画像であり、Minecraft画面ではない。柄と鍔の暗い小装飾は縮小時に弱い。

検証: 対象Python **50件**、プロセスを起動しない起動mock **4件**が成功。
実際の26.2クラスで **250モデル**の読み込みと **1,000手持ち変換**を確認。
実サーバーのbundle関数で **8,788ファイル/28武器参照**を検証した。
UI/フォント/HUD/防具を含む既存ファイルは、28武器item定義以外バイト同一。
これは対象経路の検証であり、全repoテスト成功や実機の見栄えの承認ではない。

重要ファイル/不具合の初動: Tierの絵や分離は `build_greatsword_tiers.py` のJOBSと
`processed-greatsword-tiers-v01/manifest.json`、誤ったTier表示は `build_weapon_playtest_pack.py`
のselected_keyと出力item定義を確認する。
**残る6武器系統はTier共通原稿。全動画確認・全系統のTier別作画・参考と同等の品質は未達成。**

### 魔杖の構え/放出の可動化（2026-09-09）

魔杖のv02原稿は変更せず、左右の薄い枝をそれぞれ別の付け根で開閉させる。
`build_pixel_armament_pack.py` の `staff_fins` は原稿上の幾何マスクだけを分け、
前後面を不透明画素の矩形へ分割する。別の枝や結晶が重複表示されないようにするためであり、
厚いピクセル立方体の積み重ねではない。表面は同一PNG/UV、最大2面の薄い要素を維持。

- 構え: 左9度/右-14度へ滑らかに開き、結晶は上0.9/手前0.65モデル単位、Y軸18度。
- 放出: 構え最終ポーズと同じ状態で始まり、6ポーズで収束。先頭の飛びをなくした。
- 柄・石突き・共有の首元は固定。待機では枝は動かず結晶だけが浮遊。
- 既存のfloat0と構え/放出通知を利用。新しい攻撃判定やクライアント処理は追加しない。

原稿分離 → 薄い面/付け根 → pose → 任意テストパック、という流れ。
重要ファイルは `build_pixel_armament_pack.py`。枝のずれは `staff_fins` の原稿境界/付け根、
動作の飛びは `pose` のopening値、絵の重複は `trace_painted_faces` とUVを最初に確認する。
`.tools/pixel-armament-pack/prepared.png` / `actions.gif` は保存済みモデルの確認用正投影。
通常RP、起動中のゲーム、防具、UIには未反映。実機の動き/見栄えは未検証。

追加4件は原稿画素の重複/欠損、左右の開く向き/固定支点、柄の不変性、構え末尾と放出先頭の
一致、全25ポーズの出力を検査。既存41件と合わせて45件のPython検証が成功。
起動mock 4件、実際の26.2で175モデル読み込み/700手持ち変換、実際のサーバーbundle処理
（8,698ファイル/28武器参照）も成功。ゲームやサーバーを起動する検査ではない。
観察根拠と実装判断は [Cryomancer/Paladinの部分確認](../../../docs/research/samusdev-class-phrases-2026-09-09.md)。
この開閉を参考動画の武器が行っていた、と主張するものではない。

### 弓・魔杖の作画改稿（2026-09-09）

組み込みimagegenによる `sources/bow-v02.png` / `sources/staff-v02.png` を追加。
旧v01原稿は保存したまま。入力画像の役割、全文プロンプト、生成ファイル名/SHAは
`generation-revisions-v05.json`。CLI/APIフォールバックは使用していない。

- 弓: 暗い緑の輪郭を残し、苔色の帯を明るく/太く、柄の上下の金具を明確に描き直した。
  native原稿は28×92画素（32×128atlas）。弦は描き込まず、可動モデルの2本の面を継続使用。
- 魔杖: 開いた2枚の装飾と結晶を大きくし、柄に紫の側面を描いた。
  横幅20→27画素、縦92/物理高30は維持。12色の32×128atlas。
  結晶の分離を色だけに依存させず、指定領域の不透明画素をすべて移動側に含めた。
  動いた後に結晶の輪郭だけ残ることを防ぐ（最細の黒縁自体は縮小時に失われる）。
- 両原稿ともRGBのチェック背景だったため、許可済みの色/領域判定で除去し、
  foregroundだけを縮小してalpha 0/255へ変換。解像度を上げて精密感を足していない。

指定の [The Dark Sister](https://x.com/MatE312001/status/2096478906299699459) の映像を再確認し、
暗い本体と明るい面の分離を基準にした。
[Phantom Bow](https://www.youtube.com/watch?v=hHji1r2UXAE) では5秒の接写を追加確認。
暗い本体の縁に、薄黄緑/青緑の大きい画素の帯が通る。白飛び/発光の撮影効果は再現保証しない。
今回も部分確認であり、全動画視聴の残件を完了扱いにしない。

2つのprocessor → 分離PNG → 既存native生成 → 任意テストパックの経路へ反映した。
可動弓の接続、175モデルのparser、700手持ち変換、パックbundleも改稿後に再検証した。
通常パック/起動中ゲーム/防具/UIは未変更。最初に見るファイルは各processorの
JOBS（原稿/SHA/分離位置）、表示確認は `display-review.png` とnative `actions.gif`。

**縮小時の明色帯と頭部の読み取りは改善したが、GUI倍率2では装飾の消失が残る。
リファレンス同等の作画や実機の最終feelを確認済みとは扱わない。**
41件の対象Pythonテスト＋4件の起動mockを使用。
別途 `test_pixel*.py` の広い指定で既存 `test_pixellab_pipeline.py` も実行され、
Windowsのsymlink権限とPOSIX 0600前提で4 failure/2 errorが出た。
今回その既存経路/権限チェックは変更していない。全テスト成功とは報告しない。

### 7武器の表示位置補正（2026-09-09）

`pixel_weapon_display.py` で、武器ごとの描かれた握りをモデルの基準点にする。
魔杖は末端の石突きではなく中央の柄、弓は握り、聖典は綴じの下側を使う。
通常/三人称それぞれの指定点へ平行移動を解き、全アニメーションで同じdisplayを保つ。
左手の回転をこちらでも反転していた二重反転を修正した。
26.2の `ItemTransform.apply` 自体が左手のrotation.y/zとtranslation.xを反転するため、
モデルJSONの左右のrotationは同じ値でよい。

GUIは静止形状の外接範囲を回転後に中央へ合わせ、XYを14単位以内へ等比縮小。
額縁用fixedは12単位以内。聖典は斜めにせず正面。PNG/UV/作画は変更していない。

確認:

- Python 40件と起動mock 4件が成功。追加5件は左右の握り、回転順、全175ポーズの
  display固定、GUI/fixedの中央配置/非伸長、原稿の握り領域を検査。
- `test_pixel_weapon_display.py` を直接実行すると `.tools/native-cuboid-check/display-contract.json`
  を生成。`CheckNativeWeaponDisplay.java` に渡し、実際の26.2クラスで全175ポーズ×4手持ち
  コンテキスト＝700点の変換を確認した。クライアントの起動/操作/改造はしていない。
- `CheckNativeCuboidModels.java` で175モデルの読み込みと不正軸の拒否を再確認。
  実際のサーバーbundle処理も8,698ファイル/28武器参照を再確認。
- `preview_pixel_weapon_displays.py` の `.tools/weapon-playtest-resources/display-review.png` は
  保存済みJSON/PNG/displayからGUI倍率2/4相当を正投影した確認画像。ゲーム画面ではない。
  見切れ/偏りはなくなったが、低倍率では弓/杖の暗く細い部分が弱い。
  リファレンス同等の視認性/作画品質を達成したとは扱わない。

主な流れ: 原稿内の握り → nativeモデル座標 → 左右の表示変換 → 静止形状のGUI配置 →
任意テストパック。重要ファイル/ずれの初動調査先は `pixel_weapon_display.py` と
`display-contract.json`、縮小表示は `display-review.png`。
**指定点への数学的な一致と、実機で掌に自然に収まることは別。後者は未確認。**
稼働中ゲーム/通常RP/防具/UIは変更していない。Tier別原稿・全動画確認・品質一致は引き続き未完。

### 武器だけの任意テスト起動

**防具はユーザーの中止指示により制作終了。既存防具を変更/削除しない。**

`scripts/build_weapon_playtest_pack.py` は、現在installDist済みのサーバーJARに入っている
パックを土台に、7武器のnative資産を追加したテスト用スナップショットを作る。
出力先は `.tools/weapon-playtest-resources/core-ui-pack/` と同フォルダのzip/report。
通常のソースRP、インストール済みJAR、稼働中の配布パックは変更しない。

28個の既存武器item定義（7系統×T1〜4）だけを新しいモデルの選択グラフへ差し替える。
従来の装備IDとfloat0による構え/放出通知をそのまま使えるため、性能/判定/保存形式/
クラス共通基盤への変更はない。新しい専用テクスチャと325モデルも同じパックに含める。
UI・フォント・HUD・防具を含む他の既存ファイルはバイト単位で同一。
**大剣・短剣はT1〜4が別原稿。他5系統のT1〜4は同じ確認用原稿を使う。全Tier作画の完成を意味しない。**

準備:

```powershell
python scripts/build_weapon_playtest_pack.py
python scripts/test_weapon_playtest_pack.py
```

ユーザーの次回起動要求時に、既存サーバーを正規に停止した後だけ:

```powershell
.\scripts\start-core-loop.ps1 -JavaHome <Java25のパス> -WeaponArtReview
```

スイッチなしは従来通り。スイッチありは確認用リソースディレクトリをclasspathの先頭に置く。
ポート使用中なら起動を拒否し、既存プロセスを終了しない。installDist更新後の古い
スナップショットもJARのSHA256照合で拒否する。実行中に切り替える機能ではない。

4件のパック検証が成功。さらに `CheckWeaponPlaytestResources.java` から実際の
`CoreUiPackServer` のbundle処理をHTTPサーバー起動なしで呼び、8,698ファイルと28武器参照を確認した。
`test_weapon_review_launch.ps1` はプロセス起動をmockし、通常/確認モードのclasspathと
使用中ポート/古いスナップショットの拒否を検査する。実機表示の成功とは別の検証。

最初に見る場所: 新モデルが出ない場合は起動時の確認モード表示 → reportのJARハッシュ →
`core-ui-pack/index.txt` と `items/weapons/<family>_t<tier>.json`。
重要ファイルは `build_weapon_playtest_pack.py` と `start-core-loop.ps1`。

### 追加4種のnative可動モデル（原稿分離の次工程）

`scripts/build_specialist_armament_pack.py` を追加し、下記の4種の分離PNGから
薄い表裏面と輪郭の側面を生成。通常RPとは別の
`.tools/specialist-armament-pack/projects-specialist-armaments-review.zip` に出力した。
PNGは元の分離PNGをバイト同一でコピーし、再度拡大/塗り替えしていない。
弦だけは弓の淡色を使う1画素テクスチャと、2枚の細い両面ストリップで作る。

- 弓: 握りは固定し、上下の弓身がそれぞれ最大12度回る。変形後の弓先から弦の両端を
  計算し、引いた中心点へ接続する。弦だけを曲げた静止弓ではない。待機時は静止。
- 戦槌: 本体を固定し、空洞の結晶を最大35度回して前へ出す。打撃の軌跡やダメージは付けない。
- 聖典: 暗い表紙/裏板を残し、中央の綴じを支点に左右のページを36/24度開閉する。
  ページの柄は追従し、下に同じ柄が残らない。裏表紙の専用作画はまだない。
- 星環杖: 柄を固定し、環が32度、中心星が逆向き40度回りながら前へ出る。
  環を塗りつぶした円盤に変えず、穴と開いた切れ目を保つ。
- 待機12/構え6/放出6、静止1を各4種に出力。手持ちのみfloat0で選択し、その他の表示は静止。
  全ポーズ末尾は静止と同一。再生時間/ゲームへの登録はまだ接続していない。

正面/斜め/側面/背面は `model-review.png`、構え最大は `prepared.png`、
構え→放出は `actions.gif`。実際に書き出したJSON/PNGから描いたもので、ゲーム画面ではない。
`test_specialist_armament_pack.py` の7件＋原稿と先行モデルの24件が成功。
弦の3接続点、左右ページの支点、環/星の逆回転、固定部分の不変性、全100ポーズ、
UV/座標、ファイル参照、通常RPとの分離を検査している。

さらに `scripts/CheckNativeCuboidModels.java` をJava 25のソースファイル実行で起動し、
実際の26.2 `CuboidModel.fromStream` で先行3種を含む175モデルを読み込めた。
不正な回転軸を入れた対照JSONは拒否された。クライアントの改造/起動/操作はしていない。
既存の26.2クライアントとライブラリのclasspathを使用し、CPU2/Xmx256mで検査した。

[公式1.21.6変更履歴](https://www.minecraft.net/en-us/article/minecraft-java-edition-1-21-6)で
22.5度刻みの制限解除を確認し、この新規モデルでは範囲内の細かい角度を使用する。
旧モデルの制限コメント/離散ポーズはこの変更では書き換えていない。

**未完了:** ゲーム内での持ち方とサイズ/タイミング、元参考との品質比較、専用背面作画、
全Tierへの展開、通常装備への適用、防具の作り直し、全動画の精査。
ゲームをテスト中のサーバーと現在の装備は変更していない。
原稿manifestの `native_models_exported:false` は原稿処理の成果範囲を表し、
今回のモデル出力結果は別の `.tools/specialist-armament-pack/report.json` に記録する。

重要ファイルは `build_specialist_armament_pack.py`。動作の崩れはその `pose` と
`bow_points`、読込失敗はnativeパーサー検査と出力JSONを最初に確認する。

### 追加4種のピクセル原稿と分離パーツ

弓・戦槌・聖典・星環杖を `processed-specialists-v01/` に追加。
元画像4枚は `sources/` に改変せず保存し、内蔵画像生成の全プロンプトと
不採用の戦槌初稿/修正の記録は `generation-batch-v04.json` に保存した。CLI/API未使用。
実原稿は4枚ともRGBの市松背景だったため、許可済みのPython処理で実透過にした。

- 弓: 27×92画素の作画、32×128のPNG。上下の弓身/握りを分離。弦はまだ未制作。
- 戦槌: 29×84画素、64×128のPNG。長い槍先の初稿を不採用にし、短い冠状の打撃頭へ修正。
  本体と中心結晶を分離。最終的な立体の重量感は未確認。
- 聖典: 59×48画素、64×64のPNG。左右ページ/表紙と綴じ/ページ下の暗い裏板を分離。
  白いページを背景として消さず、ページを動かしても同じ模様が下に残らない構成。
- 星環杖: 35×92画素、64×128のPNG。柄/開いた環/中心星を分離。環の隙間は実透過。
- 各16色、二値アルファ、補間/ディザなし。既存3本のパレットは変更しない。
  小さい紫の星や赤い栞を近似色へ潰さないよう、色差を優先した初期色選択を使う。
- **追加4種はピクセル原稿と分離PNGまで。nativeモデル/アニメーションは未出力・通常RP未適用。**
  弓の弦と引き絞り、ページの開閉、環と星の個別動作は次工程。静止絵だけで完成扱いしない。

再生成は `python scripts/process_specialist_armament_art.py`、検証は
`python scripts/test_specialist_armament_art.py`。追加6件と既存18件に成功。
原稿SHA/全レイヤー再生成一致、淡色保持、色数/解像度、パーツ合成一致、
星と弓の宝石と赤い栞の色、穴の透過、通常RPからの分離を検査した。
確認画像は `.tools/texture-first-specialists/pixel-review.png`（ゲーム画面ではない）。
重要ファイルは `scripts/process_specialist_armament_art.py`。
欠け/色違いは `processed-specialists-v01/` のPNGとmanifestを最初に見る。

### 先行3種のモデル出力

ユーザーから透過処理・固定ピクセル解像度・減色の許可を得て、剣・短剣・杖を加工した。
下部の「許可待ち」「RGB不採用」は当時の記録。加工前の原稿はそのまま保存している。
通常RPにはまだ適用していない。ユーザーのテストプレイ中はサーバーを更新/再起動しない。

- 原稿: 既存 `sword-material-study-v01.png` と `sources/dagger-v01.png`、`sources/staff-v01.png`。
  内蔵画像生成によるもの。元プロンプトは `prompt-v01.txt` / `generation-attempts-v03.json`。
  CLI/API fallbackは使用していない。全原稿のSHAを固定して改変を検知する。
- 加工済み: `processed-v01/`。剣32×128・短剣32×64・杖32×128の実PNG。
  不透明部分は剣22×92・短剣20×60・杖20×92。余白を高解像度の作画と混同しない。
  赤系の2本は共通16色以内、杖12色。背景は実アルファ0、前景は255。ディザ/補間なし。
- RGB原稿の背景抽出は、実際に確認した明るい無彩色の市松背景専用。
  その判定を白い武器等へ汎用適用しない。縮小セルの色は前景だけの中央値から選び、
  背景色を刃へ混ぜない。制作済みの絵の輪郭を縮小し、別の武器をコードで描き直してはいない。
- 宝石は別PNGへ分離。剣/短剣の下には暗い台座色を置き、杖の下は透明な空間にする。
  宝石が動いても下に同じ宝石の絵が残らない。
- 形状: 刃/薄飾りは0.14〜0.18、握り等0.45〜0.9の厚さ。アルファ輪郭の側面のみ。
  不透明ピクセルを立方体で埋めない。3本の物理寸法に対するピクセル密度は同じ。
- 各武器に待機12・構え6・放出6ポーズ。独立した宝石の浮遊/前後移動であり、
  参考映像の全機構を再現したという意味ではない。手持ち以外は静止。
- 剣202要素、短剣138、杖156。旧高解像度剣の781要素から減ったが、数だけで品質合格にしない。

処理と確認:

```powershell
python scripts/process_armament_art.py
python scripts/build_pixel_armament_pack.py
python scripts/test_pixel_armaments.py
python scripts/test_texture_first_sword.py
```

独立したnative RP: `.tools/pixel-armament-pack/projects-pixel-armaments-review.zip`。
アイテムキーは `projects:weapons/pixel_greatsword` / `pixel_dagger` / `pixel_staff`。
通常サーバーの許可リスト/装備への登録はまだしていないので、現在のプレイには出現しない。
`model-review.png` / `actions.gif` は出力JSONとPNGから描いた確認用。ゲーム画面ではない。

新規8件+既存10件成功。元原稿SHA、再生成一致、実透過/色数、宝石分離、全ポーズの
UV/座標/参照先、静止復帰、zip構成、通常RPに混入していないことを検査した。
重要な処理は `process_armament_art.py` → `processed-v01/manifest.json` →
`build_pixel_armament_pack.py`。絵の欠けは最初の加工PNG、形やポーズは出力JSONから調べる。

残件: 裏面専用作画はなく正面を再使用。側面/握りの見やすさと実際の手持ちサイズの
確認が必要。現時点で参考と同じ完成品質とは認定しない。残りの武器、防具、全動画確認も未完了。

`sword-material-study-v01.png` は内蔵画像生成による原稿。CLI/API fallbackは使用していない。
参考動画の公開画面をスタイル参照として使用した。元作品のテクスチャファイルやメッシュを取得したものではない。
生成プロンプト全文は [prompt-v01.txt](prompt-v01.txt)。

## 原稿の検査と採用状態

- 724×2172 / RGBA。生成されたアルファは加工せず保存。
- 刃の曲がり、刃の明色帯、鍔の細い突起が、箱の形でなく絵の輪郭で表現されている。
- 見かけの大きいピクセルと実際の画像寸法は異なる。正確な32×96のピクセル原画ではない。
- 輪郭に微細な色の混入と不均一な粒度が残る。ゲーム用完成テクスチャとは認定しない。
- 通常RP/index/プレイヤー装備からの参照なし。下記の独立した薄型モデル試作を追加。
  宝石の独立した厚みと12アクションポーズも追加。ゲーム内表示/成立済みスキルとの接続は未検証/未実装。
- この原稿を次の基準に置き換えない。最終比較相手は常にユーザー指定の元参考。

## 実装へ進めるときの条件

1. 実サイズのピクセル輪郭・限られた色面を確定する。高解像度画像をそのまま貼って完了にしない。
2. 刃と薄い装飾の絵を薄い面で保ち、握りや宝石にのみ必要な厚みを付ける。
3. UVと厚みが正面の絵を壊さないか、側面と背面も確認する。
4. 一人称/三人称/GUIの実モデルを比較する。美化したコンセプト画像だけで合格にしない。
5. 全職への展開でも同じピクセル密度と質感を保つ。既存の箱組み共通素材方式へ戻さない。

## テクスチャの輪郭に沿った薄型モデルの試作

`sword-mesh-v01.json` → `scripts/build_texture_first_sword.py` →
`.tools/texture-first-native/assets/projects/` の native item/model/texture。
出力は通常RPと分離しており、プレイヤーの武器は変えない。

刃は厚さ0.12、鍔は0.5、握りは0.85（1/16ブロック単位）。
正面/背面は原稿のUVを保つ面、側面はアルファ輪郭に沿った線分だけ。
不透明ピクセルを1個ずつ立方体にする方式ではない。原稿はバイト単位で同一コピーする。
透明な穴も側面生成で埋めない。26.2の `ItemModelGenerator.NORTH_FACE_UVS` を
ローカルclient jarで確認し、north面の反転Uを合わせた。

再生成と検証（repo root、Pillow/numpyが入ったPython）:

```powershell
python scripts/build_texture_first_sword.py
python scripts/test_texture_first_sword.py
```

出力 `views.png` は保存されたJSON/PNGを読む正面・斜め・側面・背面の確認画像。
Minecraft画面でも完成品質の証明でもない。共通プレビューは、片面のみを持つ要素、
native面のUV方向、`shade=false` を扱えるように修正した。

初回は構造検証6件成功、側面542面+表裏用3要素=545要素、モデルJSON約100KB。
この数は完成品の推奨値ではない。原稿の微小な輪郭ノイズも拾っているため、
本番用の低解像度原画を確定して輪郭を整理する必要がある。
側面抽出のalpha閾値128はこの試作用。native描画の閾値と同一と保証しない。
宝石の独立した絵、裏面固有の作画、装備時位置合わせとゲーム内動作が残る。

## 宝石の独立面と構え/復帰の試作

元原稿の宝石領域を、設定ファイルの範囲・種点・赤系色の条件からジオメトリ用に選択。
画像は編集せず、同じ元PNGのUVを参照する。前後面も輪郭内の連続した長方形に分け、
ダイヤ形の外側に四角い板や別の金属柄を足さない。
宝石の厚さは0.4。構え6ポーズで前へ0.3、放出6ポーズで前へ0.5から静止位置へ戻る。
最後のポーズは静止モデルと同一。刃・鍔・握り・UVは動かさない。

`jewel-action.gif` は出力された12個のJSONと実PNGを読む確認アニメーション。
参考動画の武器変形を再現したと主張するものではなく、独立面を動かす経路の試作。
宝石の裏面と下に残る台座の絵は元の正面原稿を再利用しており、本番作画は未完了。
現在781要素（宝石236）、JSON約148KB。微細な輪郭由来の面が多く、軽量化も残る。

構造検証9件成功。新規検証は、輪郭内の面の欠け/重複、偽の透明背景の拒否、
全12ポーズでの元画像/非可動部分の保持、静止位置への完全復帰。

### nativeアイテムからアクションモデルを選択

当初のitem定義は静止モデルだけを参照し、12ポーズは生成されても選択できなかった。
`build_texture_first_sword.py` の `item_definition()` で、既存サーバーと同じ
CustomModelData float0の12〜17を構え、18〜23を放出へ接続した。
0〜11は静止（この原稿には待機ループ未制作）。未設定/負値/24以上も静止へ戻す。

一人称/三人称の左右の手持ちだけがこの分岐を使う。GUI・額縁・ドロップ・頭などは
常に静止モデル。`hand_animation_on_swap=false` は、ポーズ切替を武器の持ち替えとして
毎回振り下ろさないためのnative設定であり、クライアント改造ではない。

テスト10件成功。追加検査は実際に書き出したitem定義を読み、全12ポーズ・境界値・
非手持ちコンテキストの参照先とモデルファイルの存在を確認する。
これはJSON分岐の検証で、Minecraftでの見え方・実際のスキル発動との接続確認ではない。
出力は引き続き `.tools/texture-first-native` のみ。通常RP登録と装備差し替えはしていない。
問題が出たら同ディレクトリのitem定義→float0→対応するmodel JSONの順で確認する。

### 分離原稿の生成結果

内蔵画像生成で4パーツ分離と背景透明化を試したが、両方とも市松模様が焼き込まれたRGBだったため不採用。
同じ失敗を繰り返す再生成は停止。CLI/API fallbackは使用していない。
プロンプト全文と失敗内容は [generation-attempts-v02.json](generation-attempts-v02.json)。
通常RPには書き込んでいない。Pythonによる透過/固定ピクセル化/減色の許可を非同期で確認中。
返答が来るまで、その画像加工は行わない。

### 短剣・杖の別原稿（未採用）

同じDark Sisterの公開画面を材質参照に、内蔵画像生成で短剣と杖を1枚ずつ生成。
短剣は鉤状の刃と空洞を持つ鍔、杖は薄い二股と離れた結晶という異なる輪郭にした。
両方とも1024×1536のRGBで、市松模様が焼き込まれているためゲーム用テクスチャとして不採用。
粒度の不均一と色面の濁りも残り、透過だけ直せば参考同等になるという判断はしない。
杖の頭部は指定した上1/3より大きく、全長のおよそ半分を占める。

プロンプト全文・検査値・元出力SHAは [generation-attempts-v03.json](generation-attempts-v03.json)。
未採用の画像は生成先のプレビューに留め、repoのアセットや実行時参照として追加しない。
同じ透明化要求で再試行を繰り返さず、既に確認したPython加工の許可を待つ。
通常RP・プレイヤーの装備・クライアントは変更していない。
